# Clinkz — Agentic AI Penetration Testing System

## Operating Context (read every task)

**ENVIRONMENT:**
- This is a Windows machine. Claude Code runs in PowerShell. Use PowerShell-native command syntax and Windows-aware paths.
- The git pre-commit hook auto-runs ruff on commit. Use foreground commands only — no background scripts or polling loops.

**PLAN-FIRST WORKFLOW:**
- Every task begins with a brief implementation plan before any code.
- The plan must incorporate the git discipline below as standing items.
- During planning, judge whether the task resembles something where a past mistake might recur. ONLY in that case, consult `.claude/LESSONS.md` for a relevant entry. Otherwise do not open it — it is not read by default.
- After completing a task, if you encountered an error worth not repeating, append a concise entry to `.claude/LESSONS.md` (what went wrong, the fix). This is the only time you write to it.

**GIT DISCIPLINE (every push):**
- When a push contains multiple commits, write a single aggregate push summary covering all of them — don't leave assembly to the user.
- Before pushing, check whether the diff makes a structural change (adds/removes/renames a file, agent, tool, model, or config option, or alters architecture). If so, update ALL affected documentation (`README.md`, `CLAUDE.md`, `CLINKZ_V2_IMPLEMENTATION.md`, `docs/`, `CONTRIBUTING.md`) in the same push. Stale docs after a structural change means the task is not done.
- Push to origin after each commit once pre-push gates pass.
- Maintain an open pull request for the working branch against main. On each push, if a PR exists, update its description to reflect the new commits; if none exists, open one with an aggregate description covering the branch's work. The PR description is the human-readable narrative of the branch — keep it current.

## What This Is
An autonomous, multi-agent AI system that performs end-to-end black-box penetration testing. It takes a target scope (IPs/domains) as input and produces a professional pentest report as output, with no human intervention in between. Agents collaborate in real-time through an LLM-mediated Orchestrator, dynamically discovering and executing tools as needed.

## Core Architecture: LLM-Mediated Multi-Agent System

### The Orchestrator Pattern
All inter-agent communication flows through a central **Orchestrator Agent**. No agent talks directly to another. The Orchestrator:
- Receives the engagement scope and defines the mission
- Spins up phase agents dynamically as needed
- Routes messages between agents (e.g., Exploit Agent asks for more recon → Orchestrator sends task to Recon Agent → routes result back)
- Monitors progress and decides when phases are complete
- Shuts down agents when their work is done
- Can spin agents back up if a later phase needs them (e.g., re-activating Recon Agent because Exploit Agent found a new subdomain)
- Triggers the Report Agent when exploitation is complete
- Maintains the global engagement context that all agents contribute to

### Agent Lifecycle
Agents are **not** all running from the start. The Orchestrator spins them up on demand:
1. Engagement starts → Orchestrator spins up **Recon Agent**
2. Recon completes → Orchestrator reviews findings, spins up **Scan Agent**
3. Scan completes → Orchestrator reviews, spins up **Exploit Agent**
4. Exploit Agent needs more recon → Orchestrator re-spins **Recon Agent** for targeted task
5. Recon responds → Orchestrator routes result back to **Exploit Agent**
6. Exploitation complete → Orchestrator spins up **Report Agent**
7. Report done → Orchestrator delivers final output, shuts everything down

This is NOT a linear pipeline. The Orchestrator can spin up any agent at any time based on what's happening. Multiple agents CAN run concurrently if the Orchestrator decides that's optimal.

### Message Format
All agent communication uses a standard message envelope:
```python
class AgentMessage(BaseModel):
    id: str                          # Unique message ID
    from_agent: str                  # Sender ("orchestrator", "recon", "scan", etc.)
    to_agent: str                    # Recipient
    message_type: str                # "task", "result", "query", "response", "status"
    content: dict                    # Payload (task details, findings, questions, etc.)
    engagement_id: str               # Links to engagement
    parent_message_id: str | None    # For request/response correlation
    timestamp: datetime
```

### Agent Communication Flow Example
```
Exploit Agent → Orchestrator: "I found subdomain api-internal.target.com in a
    response header. I need it enumerated before I can test it."
Orchestrator (LLM reasons): "This is a recon task. Recon Agent is not running.
    I'll spin it up with a targeted task."
Orchestrator → Recon Agent: "Enumerate api-internal.target.com — ports, services,
    tech stack. Report back."
Recon Agent (runs tools, completes): → Orchestrator: "Here are the results:
    3 open ports, nginx 1.24, Node.js API backend."
Orchestrator → Exploit Agent: "Recon complete for api-internal.target.com.
    Here are the findings: [data]"
Exploit Agent continues exploitation with new intel.
```

## Agents

All v2 phase agents follow the **deterministic-steps-with-LLM-checkpoints** pattern: a fixed sequence of tool calls and code, with the LLM invoked only at named reasoning checkpoints (planning, classification, synthesis). No free-form ReAct.

### Orchestrator Agent
- **Role**: Central coordinator and message router
- **Default LLM**: Anthropic (Claude) for strategic reasoning, with a resilient fallback to Gemini and OpenAI
- **Phase shape**: Recon (sequential) → **Scan + Research + Exploit run concurrently** sharing SQLite state → Report (sequential). Exploit's only hard dependency is Scan — it starts as soon as Scan completes and never blocks on Research (whose runbook is a best-effort handoff, folded in only if already done).
- **Credit pre-flight (`llm/fallback.py::preflight_provider_available`)**: At engagement start (alongside `ensure_container_ready` / `validate_agent_chains`) the Orchestrator makes one cheap Gemini probe **when both a Gemini and an Anthropic key are present**. If it returns a depleted/unavailable signal (429 `RESOURCE_EXHAUSTED`, 503, timeout), Gemini is excluded from every agent's fallback chain for the whole engagement (`_build_agent_llms(exclude_providers={"gemini"})`) and the run completes transparently on Anthropic — logged as "Gemini unavailable — running engagement on Anthropic fallback". This detects depletion up front instead of letting each agent storm bounded 429 retries mid-pipeline, and it keeps the deterministic auth flow working: that flow makes no LLM calls, but it depends on recon's tech-stack extraction, which silently degrades under a depleted primary — routing recon to Anthropic from the start keeps default-credential discovery (and the behind-login crawl) intact.
- **Cross-phase re-spins**: Capped at `MAX_CROSS_PHASE_RESPINS = 3` per engagement
- **Does NOT**: Execute tools directly — delegates all tool work to phase agents.

### Recon Agent (v2)
- **LLM**: Gemini Flash (per-agent override `LLM_PROVIDER_RECON=gemini`)
- **Steps**: Full TCP port scan → LLM analyzes ports → service/version detection → LLM extracts tech stack → web-specific recon → LLM synthesizes summary → structured `ReconResult`
- **Tool discovery**: Always via `ToolResolver.find_tool(capability=...)` — never direct imports

### Scan Agent (v2)
- **LLM**: Gemini Flash
- **Steps**: LLM plans scan strategy → service-specific methods (HTTP / FTP / SSH / SMB / DB) → LLM reviews each tool output → LLM checks coverage sufficiency → expand via fallback chain if insufficient → structured `ScanResult`
- **Fallback chains**: `katana → gospider → hakrawler` for crawling; `ffuf → gobuster → feroxbuster` for fuzzing (declared in `tools/resolver.py::TOOL_CHAINS`)
- **SPA/API route discovery (`agents/_route_discovery.py`)**: modern single-page apps invoke their real surface (`/api/...`, `/rest/...`, path-param and concat-built `?q=` routes) from JavaScript at runtime, so an HTML/JS crawl under-reports them — notably the param-bearing routes a methodology needs (`/rest/products/search?q=`, `/rest/basket/:id`). `_scan_http_service` runs a set of pluggable **discoverers** behind the `RouteDiscoverer` protocol and unions the results into `HTTPScanResult.endpoints` (additive — it never replaces katana output; deduped by `(url, method)`). Two implementations today: `StaticBundleDiscoverer` (fetch the SPA shell → follow same-origin `<script src>` + webpack chunk `.js` refs in a bounded BFS → regex-extract route-shaped literals incl. interpolated `${host}/rest/basket/${id}` forms, with path/query param structure) and `OpenAPIDiscoverer` (probe `/openapi.json`, `/v3/api-docs`, … and parse a real spec — including **`requestBody` schemas** into `json_body` params, resolving local `$ref`/`allOf`/`oneOf`/`anyOf` with a depth bound and **rejecting remote-URL refs** as an SSRF guard; else a tight conventional-JSON-root probe **plus a curated JSON-POST body fallback**). The seam is deliberate: a future `HeadlessDiscoverer` (browser-driven XHR observation) slots in as another protocol impl with no other changes — **not built now, no browser dependency**. Param structure (fix #4 — `ParamLocation` enum + `Endpoint.param_locations`/`content_type`): **path params** are emitted as `:id` segments in the URL; **query params** ride in `Endpoint.params` (location `QUERY`); **JSON request-body params** ride in `Endpoint.params` marked `JSON_BODY` with `content_type=application/json` (the source the state-changing methodologies need for `POST /api/Feedbacks {comment,rating}`, `POST /rest/user/login {email,password}`). When **no spec is served** (the real Juice Shop case — its `/api-docs` is Swagger-UI HTML over a partial `/b2b/v2` doc, not a parseable spec), the `_KNOWN_JSON_POST_BODIES` fallback emits those conventional mutation endpoints as `POST` with `json_body` params, **gated on a SPA-200-safe existence probe** (a bare `200 + text/html` is the SPA catch-all and is skipped, so no phantom route is invented). Safety: regex/JSON only (no `eval`), bounded bundle count/bytes and spec size + body-schema depth/field caps, same-origin-only bundle fetches (defence-in-depth over the tool scope check), and the SPA-200 trap is guarded (spec/root probes require a JSON content-type + parseable body, never a status code alone). Discovery requests carry the engagement session via `_discovery_http_get` (cookies + JWT/bearer headers), which also backs endpoint enrichment. **Recon→scan handoff (resolved):** the prior "Juice Shop hands Scan an empty service list" reading was a stale, pre-fix diagnosis — nmap labels the port-3000 Juice Shop web app `ppp`, which read non-HTTP until the known-web-ports `is_http` fix (`95db88f`), so `_step_execute_scans` routed it to the unsupported-service skip and `service_scans` came back empty. Post-fix recon emits the port-3000 HTTP service (`is_http` True from the port set regardless of the mislabeled name) and scan dispatches the crawl/discovery; locked in by `test_recon_emits_http_service_for_mislabeled_web_port` and `test_scan_dispatches_mislabeled_web_port`. Remaining Juice Shop endpoint delivery is now purely a discovery-layer concern (bundle/OpenAPI), not the handoff.
- **Crawl-safety (`agents/_url_safety.py::is_state_changing_url`)**: discovered links that mutate the target's security posture — WAF/security-level toggles (e.g. DVWA `security.php?phpids=on`), logout — are never visited (enrichment) nor persisted as endpoints. All v2 phases share one engagement session, so following such a link silently poisons it for every later phase. The concrete regression: the enrichment step GET-visited `security.php?phpids=on`, enabling DVWA's PHPIDS WAF, after which every Exploit-phase injection payload returned the generic "Hacking attempt detected and logged." block page and verification confirmed nothing.

### Research Agent (v2)
- **LLM**: Gemini 3.1 Flash-Lite (GA) — `LLM_PROVIDER_RESEARCH=gemini`, model pinned via `GEMINI_RESEARCH_MODEL` (default `gemini-3.1-flash-lite`; never the shut-down `-preview` variant). Fast profile with Anthropic/OpenAI fallback.
- **Live web research via native Gemini Search Grounding**: `GeminiClient.research()` attaches a Google Search tool, so `RuntimeResearcher` retrieves live CVEs/writeups natively. `runtime_research.py` is retained because it *complements* grounding (it adds NVD structured CVE data, then delegates to the grounded `research()`) — it is not a separate/duplicate search path.
- **Runs concurrently** with Scan and Exploit. **Exploit does NOT wait for Research** (Exploit depends on Scan): Research's runbook is folded into Exploit only if Research has already finished; otherwise Exploit starts immediately and Research is collected for the report afterwards.
- **Rate-limit aware**: bounded retries + exponential backoff capped at `LLM_RETRY_MAX_DELAY` then provider fallback (no 503 storm); a configurable RPM ceiling (`GEMINI_MAX_RPM`, default 30, Tier-1 sized); and a hard wall-clock budget (`RESEARCH_TIME_BUDGET`, default 180s) after which it returns whatever techniques it has gathered.
- **Steps**: Query persistent KB for tech → web-search new vulns (CVEs, writeups) → LLM synthesizes techniques → query related techs → LLM adapts past techniques → persist to engagement runbook AND `clinkz_knowledge.db`
- **Mid-engagement hook**: `research_additional()` for techs Scan discovers later

### Exploit Agent (v2)
- **LLM**: Anthropic (Claude Opus) — pinned by `LLM_PROVIDER_EXPLOIT=anthropic`
- **Steps**: LLM plans exploits from scan + research data → execute deterministic `_test_*` methods by tier → LLM reasons through results → adaptive retry/bypass → record capability outcome to the persistent KB (Layer-2 learning loop; see the write-back bullet below)
- **Adaptive methodologies (W2.1)**: All 19 `_test_*` methods are now adaptive multi-phase methodologies, not deterministic one-shot skills. The payload-injection family (`_test_xss_reflected`, `_test_xss_stored`, `_test_xss_dom`, `_test_sqli`, `_test_nosqli`, `_test_ssti`, `_test_xxe`, `_test_cmdi`, `_test_lfi`, `_test_file_upload`, `_test_idor`, `_test_open_redirect`, `_test_javascript_attacks`) uses the six-phase reflection/fingerprint/synthesize/verify pattern with LLM-driven payload synthesis. The behavioral family (`_test_csrf`, `_test_brute_force`, `_test_weak_session`, `_test_security_headers`) uses the four-phase hypothesis/observe/analyze/emit pattern. CMDi phase-1 candidacy also runs a reflection-guarded **echo-canary** probe (`<sep>echo <canary>`) so command injection surfaces even when the base command writes only to stderr (e.g. DVWA's `ping <bad-host>`, dropped by `shell_exec`) and bare-separator probes look inert. CMDi phase-5 verification is reflection-honest in the same spirit: an indicator surfacing in an **error response** (HTTP 5xx or a shell/stack-trace error page) is rejected as input reflection — genuine command output comes back in a normal 2xx/3xx response in command-output position — and the echo-scaffold reflection guard is **encoding-robust** (it decodes `echo%20<canary>` / entity forms before checking). This closes the Juice Shop a07df54b phantom, where three "high" RCEs confirmed off a 500 page that merely reflected the `;echo PWNED` payload string (Node, no shell sink).
- **NoSQL injection (`_test_nosqli`) — first new Tier-1 primitive (the roadmap's `docs/ROADMAP.md` "current position")**: mirrors the SQLi six-phase injection-family shape; only the operators/carriers differ (MongoDB `$ne`/`$gt`/`$regex`/`$where` vs SQL clauses). Models live in `models/methodology.py` (`NoSQLContext`, `NoSQLInjectionType`, `NoSQLPrimitives`, `NoSQLMethodologyResult`). A **dedicated probe carrier** (`_nosql_send_probe` / `_build_nosql_json_body`, leaving the shared string-only `_send_probe` untouched) is the key piece: NoSQL operator injection needs a **nested operator object** (`{"$ne": null}`) as a JSON-body field *value* (not a stringified literal) — `_http_post_json` already serialises `dict[str, Any]` and carries cookies + JWT. Carriers: **(A) JSON-body operator object** (`PATCH /rest/products/reviews {"id":{"$ne":-1}}` — Juice Shop "NoSQL Manipulation", confirmed by a `modified`-count widening over the benign baseline) and **(B) string `$where`** in a path/query segment (track-order / `sleep(N)` "NoSQL DoS", confirmed by a time delta). Phase 2 classifies the carrier from **NoSQL-only signals** — operator confirmation requires a NoSQL error signature OR a count-based match-set widening (a bare body diff is rejected so a DVWA `id[$ne]=` PHP "Array to string" warning is not mistaken for NoSQL), and `$where` needs a NoSQL error or a non-SQL 5xx. Phase 3 returns **no candidate type** when no NoSQL signal exists, so NoSQL is N/A by construction on a SQL/PHP stack (DVWA — no false emission). Phase 5 is verification-honest like SQLi/CMDi: a marker/operator echoed in a 4xx/5xx body, or a SQL error, never confirms. **Juice Shop's login is SQL injection, not NoSQL**, so the canonical NoSQL gate is the reviews-manipulation surface — not a login `{"$ne":null}` bypass (that auth-bypass type is implemented but correctly does not fire there).
- **SSTI (`_test_ssti`) — second new Tier-1 primitive**: mirrors the SQLi/NoSQL six-phase injection-family shape; the carrier is template syntax and the gadgets are engine-specific. Models live in `models/methodology.py` (`SSTITemplateEngine`, `SSTIExploitationType`, `SSTIPrimitives`, `SSTIMethodologyResult`). SSTI payloads are **strings**, so the shared string-only `_send_probe` carries them unchanged (no dedicated carrier, unlike NoSQL). Phase 1 sends **polyglot arithmetic probes** (`{{a*b}}`/`${a*b}`/`#{a*b}`/`<%= a*b %>`/`{a*b}`) with **randomized factors**; candidacy is *evaluation* (the product rendered AND the literal wrapper not reflected verbatim), so literal reflection on a non-template stack (DVWA) is correctly N/A. **Read-back aware** (`_ssti_observe`, mirroring stored-XSS): a body/form param submitted via POST can render its eval second-order on a GET of the page — the Juice Shop **Pug `/profile` username** case (`POST /profile` 302-redirects; `GET /profile` renders `#{a*b}` → product). Phase 2 fingerprints the **engine** (SSTI's "SQL-dialect"): `#{}`→Pug, `<%= %>`→EJS, `${}`→Freemarker, `{{}}`→Jinja2-vs-Nunjucks/Twig via `{{7*'7'}}` (`7777777` Python string-repeat vs `49` JS coercion). Phase 4 **conditions synthesis strictly on the fingerprinted engine** (the SQLi lesson: don't synthesize for the wrong engine). Phase 5 is verification-honest like CMDi: an arithmetic eval must render in a normal (sub-400) response; an RCE **echo-canary** (`echo <token>`) must land in command-output position — a canary in a 4xx/5xx/error body, or an echoed `echo` scaffold, is reflection not execution (reuses `_cmdi_body_has_error` + the encoding-robust scaffold guard). Severity **high** (eval) / **critical** (RCE: Pug/EJS/Nunjucks Node `child_process` gadget, Jinja2 `__class__` chain, Freemarker `Execute`). Phase 3 returns **no candidate type** when nothing evaluates, so SSTI is N/A by construction on DVWA — no false emission. **Literal-reflection coexistence (live-verified fix):** Juice Shop's profile renders the username *twice* — the evaluated product in a `<p>` AND the raw literal in an input `value=` — so eval detection is **baseline-anchored** (the product must be absent from a benign baseline body, never gated on "literal present") and RCE confirmation **strips the `echo <canary>` scaffold** (encoding-robust) so a *bare* canary confirms even when the whole gadget is also reflected verbatim. The earlier "reject if the literal/scaffold is anywhere in the body" guard silently suppressed real findings and was caught only by running against an eval-enabled instance. **Juice Shop default-Docker is an expected justified non-finding**: the username-eval is gated behind `isChallengeEnabled(usernameXssChallenge)`, and `challenges.safetyMode=auto` + the challenge's `disabledEnv:[Docker]` disables it, so `#{a*b}` renders literally and nothing emits — verified end-to-end against a `safetyMode=disabled` instance (high-severity Pug `expression_eval` finding).
- **XXE (`_test_xxe`) — third new Tier-1 primitive**: mirrors the SQLi/NoSQL/SSTI six-phase injection-family shape, with two deliberate structural differences. (1) **Endpoint-scoped, not parameter-scoped**: XXE has no injectable named param — the whole XML *document* is the payload — so `_test_xxe` runs the methodology once per page (like `_test_file_upload`), not in a `for param` loop; `candidate_param` is a synthetic carrier label (`file` / `xml_body`). Models live in `models/methodology.py` (`XXEExploitationType`, `XXEParserCapability`, `XXEMethodologyResult`). (2) **New carrier**: a raw XML body is the one shape `_send_probe`/`_http_post_json` cannot express, so a new `_http_post_xml` (raw `application/xml` body) plus an `_xxe_send_probe` dispatcher carry it — multipart **file upload** (reusing `_http_post_multipart`, field `file`, filename `*.xml` — Juice Shop's `POST /file-upload`) for upload-shaped endpoints, raw body otherwise; `_send_probe` is left untouched (the NoSQL discipline). Phase 1 sends a **benign internal-entity probe** (`<!ENTITY t "MARKER">…&t;`) and is a candidate only if the endpoint *parses XML* (the marker expands, an XML parse-error surfaces, or a malformed/well-formed divergence) — so a non-XML stack (DVWA) is N/A. Phase 2 fingerprints the **parser-capability vector** (`XXEParserCapability`: entity-resolution / external-entity / parameter-entity / in-band-reflection) — the SQL-dialect / SSTI-engine analogue; no `entity_resolution` ⇒ phase 3 returns nothing. Phase 3 ranks `file_disclosure` / `ssrf` / `dos`; `oob_exfil` is **pruned** (no OAST collaborator wired, the same limitation as CMDi `BLIND_OOB`). Phase 4 prefers the deterministic capability-grounded build and **scope-validates** every external-entity / SSRF target (`self.scope.contains` — no exfil to arbitrary hosts; `file://` reads are the target's own filesystem). **Phase 5 is the key deviation from the prior trio: the legitimate XXE disclosure channel is a 4xx body** — Juice Shop returns the expanded entity in an **HTTP 410** (`…deprecated for security reasons: <…>root:x:0:0:…`), so unlike CMDi/SSTI a 4xx/5xx status is **NOT** a reject signal. Honesty instead comes from confirming on actual **file content** (`_XXE_FILE_CONTENT_SIGNATURES` — embedded, *not* line-anchored like `_LFI_FILE_SIGNATURES`, since the disclosure arrives wrapped) that we never sent — a reflected payload never confirms. DoS confirms on a **bounded** nested-entity parse-time delta / 503 timeout, **never a real billion-laughs** (which segfaults the Juice Shop libxml); the expansion is capped tiny (`8**4` ≈ 100 KB). Severity **high** (file disclosure / SSRF) / **medium** (DoS). **Juice Shop reachability nuance**: the XML parse + entity-expansion path is gated only by `deprecatedInterfaceChallenge` (which has **no `disabledEnv` → it runs in Docker**); the two XXE *challenges* (`xxeFileDisclosure`/`xxeDos`) are `disabledEnv:[Docker,Heroku,Gitpod]`, which suppresses only the **scoreboard banner**, not the vulnerable code. So XXE file disclosure may be a **real in-band finding even on default-Docker** *if* the container's libxml resolves the external file entity (documented-unreliable — the reason the challenges are Docker-disabled); when it doesn't, the methodology emits nothing — a verification-honest non-finding either way (the `juiceshop_smoke` gate is skip-tolerant like SSTI's). N/A by construction on DVWA (no XML parser).
- **JWT attacks (`_test_jwt`) — first Tier-2 primitive**: **not** injection-family — cryptographic token manipulation — but it reuses the six-phase scaffolding (map → fingerprint → rank → synthesize → verify → emit) with cryptographic content. Uses **PyJWT** for all token encode/decode/sign — never hand-rolled crypto. Models live in `models/methodology.py` (`JWTAlgorithm`, `JWTAttackType`, `JWTFingerprint`, `JWTMethodologyResult`). Like XXE it is **endpoint-scoped, not parameter-scoped**: the *token itself* on an authenticated request is the injection point, so `_test_jwt` runs once per page (no `for param` loop); `candidate_param` is a synthetic carrier label (`authorization_bearer` / `jwt_cookie:<name>`). **New carrier** `_jwt_send_with_token` sends `Authorization: Bearer <token>` and **drops the cookie jar** so the only credential under test is the token (otherwise an ambient session cookie — Juice Shop also sets a `token` cookie — masks whether the forged token was accepted); the shared `_send_probe`/`_http_*` family is left untouched (the NoSQL/XXE discipline). Phase 1 acquires the JWT (the captured bearer in `_session_headers`, a JWT-shaped cookie, or one harvested on the endpoint) and **baseline-anchors** that THIS endpoint validates the signature — the valid token is accepted (2xx/3xx) AND a broken-signature token is rejected (401/403); if not, it declines (so an open/cookie-authed endpoint never yields a phantom). No JWT held anywhere ⇒ N/A by construction (DVWA's PHP session cookies). Phase 2 fingerprints the token (algorithm / `kid` / claim **names** / privileged claims / expiry — **never claim values**, so the fingerprint is redaction-safe). Phase 3 ranks the attack classes (LLM checkpoint, Anthropic-pinned) with **hard preconditions enforced regardless of the LLM** (`alg_none` only if signed, `algorithm_confusion` only if asymmetric, `weak_secret` only for HMAC, `kid_injection` only if `kid` present, `claim_tampering` only if a privileged claim exists, `expired_acceptance` only if `exp` present). Phase 4 synthesis is **deterministic** (token signing is cryptographic — an LLM cannot produce a valid signature, the JWT analogue of "prefer the empirically-grounded deterministic build" from SQLi/XXE): alg:none strips the signature (+ `None`/`NONE` case-variant bypasses); `weak_secret` cracks a **bounded** common/default-secret list (`_JWT_WEAK_SECRETS`, ~25 entries — proves the secret was guessable, never an exhaustive crack) via a local signature check then re-signs; `algorithm_confusion` re-signs HS256 with a JWKS-fetched public key (in-scope JWKS probe only; declines without one — the documented-limitation pattern, like XXE `oob_exfil`); `claim_tampering`/`expired_acceptance` require a cracked key so the elevated/back-dated token is *validly signed* (an honest, distinct proof). **Phase 5 is fully in-band** — the strength of JWT vs the OOB-limited XXE: send the forged token to the gated endpoint, confirmed only when it is **accepted like the valid baseline** (a success status matching the valid token, diverging from the 401/403 reject). A rejected token never confirms. Severity **critical** (alg:none / confusion / weak-secret / kid → forgery / full auth bypass) / **high** (claim-tampering / expired). **Token redaction** (`_jwt_redact`): findings/logs carry only the decoded header + claim *names* + `<sig elided>` — never the raw token or the cracked secret (no PII/secret leakage). Per-engagement dedup (`_jwt_emitted_attacks`) emits each attack class at most once (acceptance is a server-wide property). On Juice Shop the canonical gates are the **Unsigned JWT** (alg:none) and **Forged Signed JWT** challenges; the `juiceshop_smoke` gate is skip-tolerant (build-dependent alg:none acceptance — a verification-honest non-finding when hardened). N/A by construction on DVWA (PHP session cookies, no JWT).
- **SSRF (`_test_ssrf`) — second Tier-2 primitive (the roadmap's `docs/ROADMAP.md` "current position"; next is making Tier-2/3 real)**: coerce the in-scope server to fetch internal/metadata addresses. **Parameter-scoped** like SSTI (runs per URL/fetch param) and reuses the shared string-only `_send_probe` — the internal URL is just a string param *value*, so **no dedicated carrier** (the SSTI discipline, vs the NoSQL/XXE/JWT carriers). Models live in `models/methodology.py` (`SSRFExploitationType`, `SSRFCapability`, `SSRFMethodologyResult`). **Safety model — the load-bearing scope interaction:** Clinkz only ever connects to the in-scope target; the internal address rides as the param *value* in a request to that target, and the scope-validated HTTP client checks the *request URL* (the in-scope host via `urlparse` — an internal address embedded in a query/body value is never the netloc), so `scope.contains("http://target/x?url=http://169.254.169.254/")` is **allowed** while `scope.contains("http://169.254.169.254/")` is **blocked**; `follow_redirects` defaults False so a 3xx the target returns to an internal address is never followed. There is deliberately **no `_xxe_payload_in_scope`-style restriction** on the payload — pointing at internal/metadata addresses is the whole test (the opposite of the conservative XXE-SSRF sub-type, which restricts its server-side-fetch target to in-scope). Phase 1 maps the injection point: a URL-shaped name (`_SSRF_PARAM_NAMES`)/value, or a server-side-fetch signal — supplying the in-scope origin vs a non-resolving `.invalid` control (both as param values) and observing a divergence the **value-echo cannot explain** (`_ssrf_fetch_signal` strips the injected value before the length compare, so a plain reflecting field like DVWA's `name` is not mistaken for a fetcher). Phase 2 fingerprints `SSRFCapability` — `fetch_confirmed`, `content_reflected` (**the in-band gate**: the fetched body echoed back), network schemes only. **`file://` is NOT an SSRF type** — a local-file read is LFI (a local disclosure, not a server-side network request), handled by `_test_lfi`; SSRF no longer probes `file://` or sets reflection from a file read (this removed the `/fi/?page=file:///etc/passwd` mislabel where LFI was double-reported as SSRF). Phase 3 ranks in-band types (LLM checkpoint, Anthropic-pinned) — but **with no content reflection the only outcome is `BLIND_DEFERRED`** (no LLM call). Phase 4 synthesis is **deterministic** and bounded to fixed well-known internal/metadata targets (`169.254.169.254`/`metadata.google.internal`/`100.100.100.100`, `127.0.0.1:<port>` loopback) — an LLM adds no value and arbitrary LLM URLs would be a payload-safety risk, so this is the SSRF analogue of JWT's deterministic signing; **never an arbitrary external exfil host**. Phase 5 is **in-band only**: confirmed when the response reflects internal content it should not have — a cloud-metadata signature (`_SSRF_METADATA_SIGNATURES`), or the app's own loopback content (the origin marker reflected when fetching `127.0.0.1` while the non-resolving control does not). **Reflection-honest** like CMDi/SSTI: the signature tokens are response-content the metadata service returns (`ami-id`, `AccessKeyId`), never substrings of the URL we sent, so an echo of the payload can never confirm; and **IAM exposure reports only the field name, never the secret value** (`_ssrf_signature_match` — read metadata to prove access, never exfiltrate the credential). Severity **critical** (cloud-metadata / IAM exposure) / **high** (internal-service). **Blind SSRF — confirmed out-of-band (P6) when a collaborator is wired, else deferred:** a confirmed fetch with no in-band reflection is no longer a dead end. When a **healthy** OOB collaborator is provisioned (`OOB_COLLABORATOR_MODE != disabled`; see the P6 bullet below), `_test_ssrf` fires the blind set through `_ssrf_oob_confirm_batch` (fire-and-reap, one shared window) and confirms on a callback bearing the probe's nonce (`BLIND_OOB_CONFIRMED`, high, raw-auditable). With no callback the outcome is `blind_unconfirmed (egress may be filtered)` — an operator research-lead, never `not_vulnerable`; with no healthy collaborator it is `collaborator_unavailable` (never `blind_unconfirmed` — a dead collaborator must not make a target look clean). With P6 **disabled** (the default) it emits NOTHING and notes `blind_suspected` — the unchanged black-box floor, never a phantom. **Juice Shop's canonical surface (`POST /profile/image/url`, `imageUrl`) is blind by design** — it pipes the fetched body to a file and 302-redirects, so the `juiceshop_smoke` gate is skip-tolerant: a justified blind-deferred non-finding unless a build reflects in-band OR a collaborator confirms out-of-band (the in-band six-phase path is unit-proven in `test_methodology_ssrf`; the OOB path in `test_methodology_ssrf_oob`). N/A by construction on DVWA (no URL/fetch param surface).

- **Out-of-band confirmation (P6) — `src/clinkz/oob/` + `_ssrf_oob_*` (docs/p6-oob-design.md, live-validated on GeoServer)**: the sixth confirmation primitive — a blind capability is confirmed when an inbound callback bearing a Clinkz-minted, single-use nonce reaches a Clinkz-owned collaborator within a wait window. **Zero-FP by construction** (§P6.2.2): the nonce is minted at dispatch and embedded ONLY in the one outbound probe, so a packet bearing it can only exist if something that received the probe executed the egress — there is no reflection channel to the collaborator, so the input-reflection confounder every in-band guard fights is *structurally absent*. **The collaborator** (`oob/collaborator.py::OOBCollaborator`) is a **receive-only** DNS (UDP) + HTTP listener — the HTTP leg returns a static 200 and never connects out; the DNS leg answers only its own advertised address, so it can never be steered into an SSRF relay. It holds a bounded+redacted per-engagement event log, a per-probe nonce→hypothesis correlation table, a preflight **health-check** (mint a token, self-hit BOTH legs, confirm the round-trip — the gate: only a healthy collaborator may turn a non-confirmation into a research-lead), and a **fire-and-reap** shared-window reaper (§P6.3.2 — send all blind probes up front, reap concurrently against one window, so K hypotheses cost one window not K). Provisioned/torn-down per engagement in the Orchestrator preflight (`_provision_collaborator`, disabled by default → unchanged black-box floor), wired onto the Exploit agent via the lifecycle. **The structural exfil guardrail (§P6.7.4 — the load-bearing safety property):** OOB payloads are built ONLY from CLINKZ-OWNED per-class templates (`oob/templates.py`) — a methodology/LLM *selects* a template (`BLIND_SSRF_URL`/`JNDI_LDAP`/`DNS_LOOKUP`), it **never authors the payload string**. `build_oob_payload()` interpolates ONLY a validated `[a-z0-9]{16,64}` nonce (`mint_nonce` = base32-of-`secrets`, metacharacter-free); it has **no parameter** for target/LLM/source data and **rejects** any nonce/zone that is not shape-valid, so an exfil template (`${env:X}.<nonce>`, a subdomain-encoded file byte) is *structurally impossible to emit* — the guardrail is enforced at the carrier, not by convention (`test_oob/test_templates.py`). P6 proves the capability *fired*; it never moves target data over the channel. **Raw-auditable** (§P6.2.4): a confirmed finding carries the `ConfirmationEvidence` pair `outbound_probe` (the probe carrying the nonce) + `confirming_excerpt` (the inbound callback bearing the SAME nonce) + a control (a fresh nonce minted but never sent → no callback). **Isolation-validated** (§P6.6) end-to-end vs a **live GeoServer 2.19.1 + live collaborator + live Anthropic exploit LLM** (`scripts/live_oob_ssrf_validation.py`, no harness, engagement `0b993c1e`): the same SSRF we confirm in-band is ALSO confirmed via the OOB path — in-band unregressed + P6 confirm (raw-auditable: the outbound probe carrying nonce `T` and the inbound callback carrying the same `T`, both in the `[HIGH] blind_oob_confirmed` finding, `content_reflected=False`) + zero-FP (egress denied by dropping the Host carrier → no callback → no finding) + the exfil guardrail (every `${env:...}`/traversal/target-host attempt refused). Written up in `docs/discovery-engine-p6-oob-validation.md` with the raw `outputs/p6-oob-ssrf/` artifacts. **Log4Shell is now BUILT on this primitive (the flagship, §P6.5)** — see the discovery Log4Shell-class bullet below and `docs/discovery-engine-log4shell-class.md`. A new nonce-only `JNDI_DNS` template (`${jndi:dns://<dns-authority>/<nonce>}`) + `OOBCollaborator.dns_authority()` carry it: the JNDI DNS provider queries the nonce **directly** at the collaborator's DNS-leg authority (`host:dns_port`), the reliable docker-mode channel with no wildcard-DNS delegation / port 53 (§P6.5.4 — the DNS leg confirms Log4Shell; the empirically-observed live channel, a documented paper-vs-live divergence from the `ldap://` shape). The `_ssrf_oob_send` mint→build→send helper was generalized to `_oob_send(…, template_id)` (default `BLIND_SSRF_URL` keeps SSRF byte-identical) so Log4Shell reuses the exact same P6 machinery — zero new proof code. The `DNS_LOOKUP` template still exists for carrier genericity, not wired to a methodology.
- **Methodology correctness — real-attacker confirmation (Juice Shop 8df94e28)**: three platform-agnostic fixes so a methodology confirms the way a real attacker does.
  - **SQLi breakout + column-count + reflection guard**: phase 2 now discovers the **breakout context** (`InjectionPrimitives.break_prefix`) — a paired boolean tautology (`<closer> OR 1=1` vs `<closer> AND 1=2`) over candidate closers (`''`, `'`, `')`, `'))`, …) finds the one that *balances* the surrounding query (parses to a 2xx and diverges true/false), and the **UNION column count** (`union_columns`) via `UNION SELECT NULL,…` enumeration. This cracks the canonical `/rest/products/search?q=` case (`((name LIKE '%<v>%' …))` needs `'))`, 9 columns) that a bare `'` never reaches. Phase 4 conditions synthesis on dialect + break_prefix + column count and **prefers the empirically-grounded deterministic build** when a breakout is known (an LLM guess that ignores the closer/column-count is the bug). Phase 5 is reflection-honest like CMDi: a union/error marker echoed in a **4xx/5xx error response** is input reflection, not data/error — rejected (the `/redirect?to=` 406 phantom, finding #14); and two further reflection guards keep a 2xx honest: the **union** branch rejects a marker that survives **only inside a verbatim echo of the submitted payload** (`_marker_only_in_payload_echo` blanks the entity- **and backslash-**de-escaped payload echo via `_normalise_for_echo_compare` — HTML-unescape + undo backslash-escaping `\X`→`X` + collapse whitespace, so a sink that `addslashes`/`mysqli_real_escape_string`-escapes the echoed input turns the payload's `'` into `\'` and STILL matches the blank — then requires the marker to remain elsewhere — DVWA `csrf/test_credentials.php`'s `Wrong password for '<input>'`, the 1b8101dd phantom, and DVWA xss_s/high `mtxMessage`'s escaped-echo phantom), and the **error_string** branch rejects an LLM indicator that was **already present in the benign baseline** body (static page chrome — a generic `"SQL"` indicator matching DVWA's left-nav menu on the `xss_r` page, the b9dc3627 phantom). The genuine `/sqli/` `First name:` UNION data row still confirms, and a real DB-error signature (`has_db_error`) still confirms error-based regardless of the indicator. `_sqli_has_db_error` combines the generic + dialect error tables so the real `SQLITE_ERROR` 500 still confirms error-based; `(status=…)` is threaded into the union/error observed strings so the Step-3b backstop catches any SQLi phantom that slips phase 5.
  - **LFI stack-awareness + file-content-only + `/ftp` poison-null-byte**: PHP wrappers (`php://*`, `data://`, `expect://`, `file://`) are probed **only on a PHP stack** (`_is_php_stack` over `self._technologies`, harvested from recon/scan/research in `run`); on Node every odd value 500s as "Unexpected path" and that divergence is no longer mistaken for wrapper support. Phase 5 confirms **only on actual file content** (`_LFI_FILE_SIGNATURES` / base64-decoded signature / source markers) — a filesystem path in a stack trace is information disclosure, not a file read, and no longer confirms LFI. A static **file-server sub-methodology** (`_test_lfi_file_server`) handles param-less routes like `/ftp/<file>`: it enumerates the directory (served listing ∪ a bounded backup wordlist) and, for a file *blocked when requested directly*, attempts the poison-null-byte extension-allowlist bypass (`<file>%2500.md`), emitting only when the bypass returns real, non-SPA file content. `_applicable_methods_for_endpoint` queues `_test_lfi` for param-less file-server paths so `/ftp` reaches LFI at all. Bounded, same-origin, regex-only.
  - **Open-redirect — dispatch fix + 3xx-Location-honest confirm + DVWA-graded + allowlist bypass**: **(dispatch — the emission blocker)** the real endpoint reaches `_test_open_redirect` only if it carries the `redirect` param, and it wasn't: a URL-only crawler (katana) emits `.../open_redirect/source/<level>.php?redirect=info.php` as a **bare** `Endpoint(params=[])`, enrichment then finds the *same* URL as a `?redirect=` link, but the scan crawl-merge kept the first-seen (bare) endpoint and **dropped** the enriched param version — so the endpoint reached Exploit param-less and the param-gated methodology never ran (this, not the confirm logic, is why the module confirmed NOTHING at every level in the pr76 real run — the earlier "live-validated L/M/H" claim was false). `ScanAgent._merge_crawl_endpoints_preferring_params` now **upgrades** the bare endpoint in place with the enriched params, realising the merge's own "prefer the version with params" intent (general fix — any query/redirect link a URL-only crawler emits raw now keeps its params). **(confirm-honesty — phantom guard)** the ONLY primary confirmation is a **server-side 3xx** whose `Location` header resolves (`urljoin`, `_redirect_target_is_attacker`) to the attacker host — `_open_redirect_phase5_verify` requires `status in {301,302,303,307,308}` + a `Location`, so a status-200 body that merely *contains* or reflects an attacker-looking string never confirms, and a same-host path containing `evil.example` (DVWA Low `info.phphttps://evil.example`, `/go?u=…`) is a justified non-finding ("a redirect to an in-site page is not the finding"). **`javascript:`/`data:` are removed** from the bypass-probe set, ranking, synthesis, the `RedirectBypassType` enum, and severity — a `javascript:`/`data:` sink is XSS / content injection (the XSS methodologies), not host-navigation open redirect. **Body-level (meta-refresh / JS `location`) redirects are demoted** to a separate, lower-severity **DOM-redirect signal** (`_open_redirect_dom_signal`, `OpenRedirectMethodologyResult.dom_redirect`), gated on the same host-resolution and NEVER on the 3xx confirm path — a LOW-severity finding tagged `(DOM/client-side)`. **(prefer-deterministic — LESSONS #18/#28, at BOTH ranking and synthesis)** the phase-2 empirical working-set beats the LLM twice: **phase 3** stable-partitions the ranked bypass types so every phase-2-confirmed primitive precedes every unconfirmed one and re-adds any the LLM dropped (`_prioritize_confirmed_bypass_types`) — otherwise a live LLM ranks a NON-working type first and phase 4/5 confirm a genuine redirect under that wrong label (the `e72ed60a` full-pipeline `appended_url` **mislabel**: a real off-site 302 through the crawled `source/low.php?redirect=` endpoint, attributed to a type phase 2 disproved — NOT a phantom; `_build_request_url` REPLACES the same-named param, so the actual request is a clean `?redirect=//evil.example`, and the earlier "doubled-param same-site 302 FALSE POSITIVE" reading was a mis-reconstructed curl of a URL the pipeline never sends); **phase 4** then uses the empirically-grounded deterministic build for a bypass type whose primitive phase 2 confirmed (`_redirect_primitive_confirmed` over `working_bypass_primitives`) and skips the LLM — a live LLM paraphrases/mangles a proven payload (wrong scheme, an appended dead `%00`), the class of live miss the keyless suite's silent LLM never exercises; the LLM stays advisory for unconfirmed primitives (ranking tie-break + non-canonical shapes). Phase 5 is confirm-honest regardless — it `urljoin`-resolves the `Location` host, so a same-host relative 302 never confirms whatever its query string contains (`_open_redirect_phase5_verify` is uniform across every bypass type; there is no payload-substring path). **(allowlist bypass — unchanged)** a redirect-shaped param (`to`/`redirect`/`url`/`next`/…) not confirmed by generic probes harvests an allowlisted token — highest-signal the param's **own** `?redirect=info.php` value, else `_harvest_allowlist_tokens` from the page-under-test / origin root / same-origin bundles (absolute outbound-allowlist URLs for Juice Shop's `/redirect?to=`, relative path/file tokens for DVWA High) — and embeds it in an attacker URL (`https://evil.example/?x=<token>`, path, fragment, `@`-userinfo), confirmed by host-resolution despite the substring allowlist (`RedirectBypassType.ALLOWLIST_BYPASS`). **Now genuinely live-validated end-to-end through the scan-merge → planner-dispatch → methodology chain with a LIVE Anthropic exploit LLM** (`ghcr.io/digininja/dvwa`, handler `open_redirect/source/<level>.php` — level in the *path*, not the cookie): **Low** (direct off-site 302 → `https://evil.example/`), **Medium** (protocol-relative `//evil.example`; absolute `http(s)://` blocked → 500), **High** (substring-allowlist bypass, `info.php` token) each confirm; **Impossible** (numeric-ID, no attacker-controlled destination) and `view_source.php` emit nothing. **Full-pipeline re-validated** (`python -m clinkz scan`, engagement `75ea835f`, DVWA low): the crawl-discovered `source/low.php?redirect=info.php?id=2` endpoint confirms a genuine `at_syntax` off-site 302 (`https://target.com@evil.example/`, curl-cross-checked), phase 3 `ranked=[at_syntax, protocol_relative, direct_redirect, appended_url, …]` (confirmed lead, appended_url 4th), correctly labeled (not the pre-phase-3-fix `appended_url` mislabel), `status=confirmed`, not Step-3b-flagged, one emission — 25 findings, no regression. The isolated `_test_open_redirect` smoke test could not catch the dispatch gap (it hands the param in directly — the false-green that shipped the bad claim), so `test_open_redirect_through_scan_merge_and_dispatch_against_dvwa` now gates the full chain. Severity **medium** (server 3xx) / **low** (DOM/client-side).
- **Impossible-level honesty — DVWA per-level regression (low/medium/high/impossible)**: four fixes so each methodology's emission is deterministic ground-truth, not LLM prose — re-validated by re-running the four affected methodologies (`_test_weak_session`, `_test_csrf`, `_test_file_upload`, `_test_sqli`) at all four DVWA security levels (matrix: low/medium confirm all four, impossible emits none, high confirms only the genuine weak-session while csrf/sqli/upload phantoms stay non-findings). The SQLi guard is the escaping-robust marker-echo clause above; the other three:
  - **Weak-session predictability is a deterministic gate** (`_deterministic_session_weaknesses`; phase-3 LLM demoted to advisory rationale only): a predictability type (`sequential`/`time_correlated`/`predictable_format`/`low_entropy`) now requires ground-truth corroboration — an integer arithmetic progression, a recent unix-timestamp window (**including a *constant* token equal to the current wall-clock second**, since fast sampling returns the same PHP `time()` value for every read — `abs(now-v)<=120`; a static numeric ID that is not "now" stays a non-weakness), a cracked md5/sha1/sha256 preimage (bounded brute force of consecutive integers + recent timestamps, `usedforsecurity=False`), or measured Shannon entropy below a bits floor. So DVWA-impossible's `bin2hex(random_bytes(20))` (160-bit) can never be labelled predictable regardless of LLM output, and a missing `SameSite` on an HttpOnly+Secure cookie is a cookie-hardening note, never a HIGH weak-session finding; low (sequential int) / medium (`time()`) / high (`md5(int)`) still confirm.
  - **CSRF rotating-session-bound-token deterministic override** (`_csrf_token_provides_protection`, phase 3, ahead of the LLM — a *third* honesty gate on top of the security-sensitivity and bearer-only gates in the shared-request-builder note below): phase 2 already captured DVWA high/impossible's rotating `user_token`, but the phase-3 LLM ruled it "effectively missing" because it rides via GET. When a token field is present, rotates across the three fetches (≥2 distinct non-empty), and is session-bound (absent on the cookie-stripped fetch — phase 2's `tokens_without_cookies`), force `protected=True`/`weakness=None` before any LLM call; GET-vs-POST transmission is at most a low-severity note, never "CSRF missing". Low (no token) / medium (Referer-only) still confirm.
  - **Upload acceptance + execution + multipart transport** (both a real transport bug and an honesty gate): the methodology never genuinely uploaded to DVWA — `_http_post_multipart` hardcoded the file field `name="file"` (DVWA's input is `uploaded`), omitted the `Upload` submit field DVWA gates on, and `http_client` sent the body via curl `-d` (strips CR/LF, corrupting multipart framing) — so DVWA received no file at any level and just re-rendered the form, whose `name="uploaded"` matched a bare `uploaded` success marker and emitted a phantom "Unrestricted File Upload" at every level. Transport now threads the form's real file-field name + submit/hidden fields (`_upload_form_fields`) through `_http_post_multipart` and switches `http_client` to curl `--data-binary` so boundary CRLFs survive. `_upload_response_accepted` anchors acceptance on a positive signal distinct from the form re-rendering — a boundary-anchored echo of the uploaded filename, an explicit success phrase, or a `Location` to the artifact (a bare form re-render is NOT accepted). Phase 5 requires ground-truth **execution** — the stored artifact fetched back must echo a self-appended canary as interpreter OUTPUT (`DIRECT_EXECUTION` and `INTERPRETER_MISCONFIG` alike; `INCLUSION_CHAIN`/`CLIENT_SIDE_ONLY` stay retrievability-grade documentation findings); a re-encoded/inert image or a source-served payload is a non-finding. Low/medium confirm via real RCE; high (image-only, no direct exec) and impossible (re-encode + token) emit nothing.
- **Bucket-B DVWA `high` vectors — cookie injection + LFI bypass-adaptive (low/medium/high/impossible re-validated)**: two contained methodology fixes that unlock the `high`-level SQLi and LFI sinks a query-only enumeration missed; both live-validated across all four security levels (matrix: low/medium confirm the genuine finding, high NOW confirms the new vector, impossible emits none; the `sqli/high` session-indirection sink is now handled by the session-vector pass documented in the next bullet).
  - **Cookie injection vector** (`ParamLocation.COOKIE` + `_cookie_send_probe`): DVWA blind-SQLi `high` reads `$id = $_COOKIE['id']` — a cookie-borne injection point invisible to `input_params` (HTML fields ∪ query keys ∪ endpoint params only). A **dedicated carrier** (`_cookie_send_probe`, the NoSQL/XXE/JWT discipline — the shared `_send_probe` is left untouched) clones the ambient auth jar and overrides the ONE target cookie with the **URL-encoded** payload (clean on curl AND aiohttp's `SimpleCookie`; the server URL-decodes `$_COOKIE`), leaving session/auth cookies ambient; `_http_get`/`_http_post` gain a `cookie_overrides` arg (via `_effective_cookies`) that never mutates `_session_cookies`. Enumeration is an **on-demand harvest** (`_harvest_injectable_cookies` in `_fetch_page`, the way a real attacker discovers the vector): find same-origin cookie-setter forms the page references (DVWA's `cookie-input.php`, basename-`cookie` heuristic), submit each once with benign values in a scratch request, and harvest the non-session `Set-Cookie` names — bounded (`_MAX_COOKIE_SETTERS`), cached per setter, **scope + `is_state_changing_url` guarded**, session-safe (never poisons `_session_cookies`). Session/auth cookies (`_SESSION_COOKIE_NAMES` ∪ live `_session_cookies` keys — PHPSESSID/security) are excluded; only app-data cookies (`id`) are injectable. `_test_sqli` runs the six-phase methodology over `page.injectable_cookies` with a **synthetic cookie-vector name** (`\x00cookie:<name>`, NUL-prefixed so it never collides with a same-named query param — both carriers are tested where they coexist); phase 6 strips the sentinel to a clean `id (cookie)` finding. The escaping-robust reflection guard applies **unchanged** through the carrier (a cookie value echoed in the body does NOT confirm SQLi). N/A by construction on impossible (parameterised query) — no phantom.
  - **LFI bypass-adaptive** (`file://` wrapper + phase-2 honesty): DVWA fi/high filters relative traversal (`fnmatch("file*", $file)`) — naive `../` returns "ERROR: File not found!". **(b)** phase-4 wrapper synthesis is generalised beyond `php://filter`: a confirmed `file://` emits `file:///etc/passwd` (absolute read that bypasses the relative-traversal filter, verified by /etc/passwd signature) and phase 3 ranks wrapper extraction first for any confirmed non-php-filter wrapper — the path that confirms fi/high. **(c)** phase-2 records a working **traversal** sequence ONLY on a real /etc/passwd content signature (a block/error page that merely diverges from the include.php baseline is no longer mistaken for a working `../`), and **wrapper** detection is signal-based not divergence (`data://` requires its inline payload rendered back; `php://input`/`expect://`, unconfirmable by a GET probe, are no longer recorded on divergence — removing the block-page false wrappers). **(a)** prefix-preserving traversal: naive-blocked + a leading token in the original value → `<token>/../../../../etc/passwd` (best-effort general capability; phase 5 still confirms only on real file content, so a token that isn't a real directory fails cleanly — no phantom). Low/medium still confirm the genuine read; impossible (whitelist) emits nothing. The now-unused `_LFI_WRAPPERS` constant was removed. **(PR#75 real-pipeline regression fix — the prior "live-validated" claim was a silent-LLM false green, traces 9f1b3950/low + e32346c0/high showed fi confirming at NO level)**: the keyless suite pins a silent LLM and only ever exercised the deterministic fallback (LESSONS.md #18), hiding two live-LLM misses. **(A) genuine-read regression** — with recon naming PHP, the live Anthropic synthesis appended a dead `%00` to the phase-2-confirmed absolute `/etc/passwd` read (null-byte poison is inert on PHP 8.5.6), so phase 5 re-probed a broken payload and low/medium stopped confirming. Phase 4 now **prefers the empirically-grounded deterministic build** (`_lfi_primitive_confirms`) whenever phase 2 confirmed the read (absolute/traversal/nul/wrapper), **skipping the LLM** for that type; and phase 3 **floats the confirmed retrieval type ahead** of the LLM ordering (and re-adds any the LLM dropped) so it is never lost from the phase-5 tried window — the same "prefer the empirically-grounded build" discipline as SQLi. **(B) file:// never probed at high** — recon/research LLM tech-extraction is flaky and silently omitted PHP (the only PHP signal on DVWA is the `X-Powered-By` *header* the harvest never reads; e32346c0 emitted `wrappers_skipped_non_php`), so the PHP-gated wrapper loop was skipped and `file://` was never sent. `_is_php_stack()` now backstops the tech list with a **`PHPSESSID` session cookie** (ground-truth PHP — only PHP's session handler sets it; Node/Juice-Shop never do, so no non-PHP false positive). Re-validated on the **real pipeline** (not the harness) at all four levels: low/medium confirm the genuine `/etc/passwd` read, high confirms `file:///etc/passwd` (phase-2 `wrappers=['file://']`, 2 real reads), impossible emits nothing — cookie `sqli_blind/high` and the Bucket-A four (sqli/upload/csrf/weak-session at low/med, clean at impossible) unregressed.
- **DVWA `sqli/high` session-indirection vector (`ParamLocation.SESSION` + `SessionVector` + `_session_send_probe`) — the phantom-prone cross-request inject-and-trigger primitive**: DVWA sqli/high reads `$id = $_SESSION['id']`, set cross-request via `session-input.php` and read by `/vulnerabilities/sqli/`'s query — a **param-less trigger** with no injectable input visible to `input_params` (HTML fields ∪ query keys ∪ endpoint params). Built **general** (injection-family-agnostic carrier), wired to SQLi as the first consumer. **Models** (`models/scan.py`): `ParamLocation.SESSION`, `SessionVector(setter_url, field)`, `Endpoint.session_setters`. **Discovery + queueing**: scan-side `find_session_setter_urls` (`_url_safety.py`) scrapes same-origin session-setter refs (incl. `onclick`/JS `popUp`) from the trigger HTML; `scan._apply_session_setter_annotations` stamps them onto the trigger `Endpoint.session_setters`, and `_applicable_methods_for_endpoint` queues `_test_sqli` for any session-annotated param-less trigger (`_dedupe_and_rank_endpoints` unions the annotation on collapse). **Carrier** (`_session_send_probe`, the NoSQL/XXE/JWT/cookie dedicated-carrier discipline — the shared `_send_probe` is untouched): a set-then-observe (`_session_write_and_fetch`) — POST the payload to the setter field (benign siblings) so the server updates the `$_SESSION` slot keyed by the ambient session, GET the trigger, observe ONLY the trigger (the setter's own echo is never the signal); rides the ambient session, never mutates `_session_cookies`. **Link gate** (`_harvest_session_vectors`/`_probe_session_setter`) establishes the setter→session→trigger channel with BENIGN values only via either signal: (a) a unique marker reflects on the trigger, or (b) **value-dependent divergence** — a benign matching value (`_SESSION_LINK_HIT_VALUE="1"`) renders a trigger result a non-matching random marker does not. Signal (b) is load-bearing because DVWA high's `ID:` echo is **row-gated** (inside `while($row=...)`): a non-matching marker yields no row, so signal (a) can never fire there. The gate NEVER confirms SQLi (benign values, no SQL) — phase 5 does, on a real effect. `_test_sqli` runs the six-phase methodology over `page.session_vectors` with a synthetic `\x00session:<field>` name (NUL-prefixed so it never collides with a same-named query/cookie param); phase 6 strips the sentinel to a clean `id (session)` finding. **SQLi echo-sink honesty (the load-bearing phantom guards — the first live HIGH run confirmed via a PHANTOM, fixed):** the session vector exposed two reflection-blind spots in the SHARED SQLi methodology (both also latent on the DVWA low/medium query path, which echoes `ID:{$id}`). **(1) Phase-2 breakout discovery** (`_sqli_discover_break_prefix`) mistook the `ID:` echo of two different payloads for a predicate divergence — the integer `user_id` coerces `'1 OR 1=1 -- -'` to `1`, so BOTH empty-closer probes match admin and differ only by the echoed payload — wrongly accepting the empty numeric closer over the genuine `'`. Fixed with `_diverges_beyond_payload_echo` (blank each body's own escaping-robust echoed payload before comparing; a genuine result-set divergence survives, so medium's real **numeric `''`** is still accepted while high's spurious echo-`''` is rejected). AND the breakout probe must terminate with `-- -` (not the bare confirmed `comment_syntax[0]`): MySQL/MariaDB only treat `--` as a comment when followed by whitespace, so a bare `--` leaves the appended `' LIMIT 1` un-commented and the genuine `'` closer errors → `break_prefix=None` (matches `_sqli_count_union_columns` / phase-4, which already hardcode `-- -`). **(2) Phase-5 union branch** accepted a union marker echoed inside an INLINE (status-200) DB error — DVWA renders `mysqli_error()` inline, so the 4xx/5xx guard never fires and the full-payload echo strip misses the partial fragment the parser reports (`near '<marker>'-- -'`). Fixed: a genuine UNION row comes from a SUCCESSFUL query, so reject when `_sqli_has_db_error(body)`; error_based confirms honestly on the signature instead. **Live-validated on the real pipeline at all four levels** (LESSONS #18 — real pipeline, not a harness): **high** confirms the session vector HONESTLY (error-based `1'` → genuine MariaDB syntax error via `POST session-input.php [id=1'] → GET /sqli/`, NOT the union phantom) alongside cookie `sqli_blind` (boolean) + fi; **medium** confirms `sqli/` (error-based) + `sqli_blind` (boolean), numeric `''`; **low** confirms `sqli/` (error-based) + `sqli_blind` (time-blind SLEEP) + brute `username`; **impossible** emits none (PDO parameterized — phase 5 finds no SQL effect; sqli/upload/csrf/weak-session all clean). N/A by construction at impossible.
- **Shared request builder — JSON-body injection (fix #4)**: a methodology says "inject value `V` into param `P`"; one builder decides **how** to carry it from the param's `ParamLocation` (threaded `Endpoint` → `ExploitTask` → `PageAnalysis`, never from LLM JSON). `_send_probe` dispatches: `json_body` → `_http_post_json` (payload in the field, benign baselines for siblings, `Content-Type: application/json`, `POST/PUT/PATCH`); an HTML `<form>` containing `P` → full-form submit (unchanged DVWA path); `form_body` → urlencoded; `path`/`query` → `_http_get` with placeholder substitution / query injection; a `cookie`-location param → the dedicated `_cookie_send_probe` carrier (see the Bucket-B bullet above). The multi-step methodologies submit through the encoding-aware `_submit_form_fields`. Crucially, the form-shaped methodologies (`_test_xss_stored`, `_test_csrf`, `_test_brute_force`) iterate `_injectable_forms(page)` = HTML forms **plus a synthesized JSON pseudo-form** built from the endpoint's `json_body` params, so a JSON API with no HTML `<form>` (e.g. `POST /api/Feedbacks {comment}`, `POST /rest/user/login {email,password}`) is reached and probed. CSRF stays honest with two gates. **(a) Security-sensitivity gate** (`_csrf_is_security_sensitive`, BUG 3): phase-1 emits only for a state change that alters the victim's security posture — a `DELETE`/`PUT`/`PATCH` REST verb, an account/email/role/delete/transfer-class keyword in the field names or the resolved endpoint path (`action_url`, so a JSON pseudo-form like `/rest/user/change-password` is classified by its path), or a **password *mutation*** — never a *plain login*. The password signal is **gated on mutation structure** (`_CSRF_PASSWORD_QUALIFIERS`): a lone `password` field does NOT qualify on its own (engagement ec39350b sprayed a CSRF finding off DVWA `/cryptography/index.php`'s single `['password','']` input); it qualifies only with **≥2 password fields** (new + confirm) OR a change/registration qualifier (`new`/`current`/`old`/`change`/`confirm`/`update`/`reset`) in a field name or the path — so `/csrf/` + `/captcha/` (both `password_new`+`password_conf`) and Juice Shop `/rest/user/change-password` (path token) still confirm while the lone-password crypto form is a justified non-finding. A bare token-less POST is therefore NOT a finding, which stops the per-form spray (the f7a761a1 over-emission flagged guestbook/CSP/crypto-encrypt/upload) while the genuine password-change challenge still confirms. **(b) Bearer-only guard**: a state-changing JSON API authenticated purely by an `Authorization` header (no ambient cookie) is not CSRF-able, a justified non-finding; a cookie-borne *sensitive* form with no anti-CSRF token still flags. `_http_post_json` carries cookies + JWT bearer exactly like `_http_post`. The DVWA query/form path is unchanged (empty `param_locations` ⇒ `QUERY`); JSON-body handling is additive. Verified live on Juice Shop: brute-force and CSRF now find their JSON-body injection points (stored-XSS reaches `/api/Feedbacks` but is captcha-gated ⇒ justified non-finding).
- **Planning with coverage union**: `_llm_plan_exploits` first structurally dedupes and ranks the crawled endpoints (`_dedupe_and_rank_endpoints`) so canonical vulnerable surfaces (e.g. `/vulnerabilities/fi/`, `/exec/`, `/upload/`, `/brute/`) lead and crawler junk (doubled relative-link paths, source/help viewers, static assets) trails. `_dedupe_and_rank_endpoints` is also the **clean-session chokepoint**: it drops state-changing links (`is_state_changing_url` — WAF/security toggles, logout) so the Exploit phase never re-poisons the shared engagement session, no matter where the endpoint came from. The top `EXPLOIT_PLAN_PROMPT_ENDPOINTS` (default 120, was a hardcoded 50) are shown to the LLM. The LLM plan is then unioned with deterministic per-endpoint coverage (`_merge_coverage` over `_build_deterministic_plan`): every `(endpoint, applicable-method)` the LLM omits is added back (bounded by `EXPLOIT_MAX_PLAN_TASKS`), so the methodology contract ("if the vuln is present, the method MUST be queued against its endpoint") holds regardless of LLM choices.
- **Discovery plan-union seam (§2.7 — discovery-engine slice 1)**: the gray-box `DiscoveryEngine` (`src/clinkz/discovery/`) is a **third plan source** alongside the LLM plan and deterministic coverage. Its hypotheses lower to `ExploitTask`s (`DiscoveryHypothesis.to_exploit_task()`) carrying an optional per-instance `carrier_constraints` list; `ExploitAgent._merge_discovery_tasks` unions `self._discovery_tasks` into the plan after `_llm_plan_exploits`, deduped by `_coverage_key`, priority-assigned, and bounded — then dispatched/deduped through the unchanged round-robin + `_persist_finding` chokepoint. `self._discovery_tasks` is empty by default, so the default pipeline is unchanged (black-box). **Wired (slice 2):** in gray-box mode — when the engagement supplies a source tree (`EngagementScope.source_dir` + optional `discovery_base_url`) — the Orchestrator's `_build_discovery_tasks` runs `DiscoveryEngine.discover(source_dir, fingerprint, base_url)` before the Exploit phase and hands the lowered Tier-A tasks across the message bus (`discovery_tasks` in the exploit content); `ExploitAgent.run` parses them via `_parse_discovery_tasks` into `self._discovery_tasks` before planning. Discovery failures degrade to black-box (never break the run). A **carrier constraint** (`discovery/constants.py::CARRIER_ALIGN_HOST`) threads `ExploitTask → PageAnalysis → _send_probe`, which rewrites the request `Host` header to the injected url's host (`host_override` on `_http_get`/`_http_post`; the TCP target is unchanged) — satisfying a server-side guard that compares url-host vs `Host` (GeoServer CVE-2021-40822). Validated end-to-end by `scripts/live_geoserver_discovery.py`; the paper-vs-live divergences (notably the SSRF confirmation-oracle app-mount-base fix) are written up in `docs/discovery-engine-slice1-validation.md`. **Transfer (slice 2, live-validated):** the same engine finds + confirms a second, different-codebase SSRF — Apache Solr 8.8.1 RemoteStreaming `stream.url` (Vulhub `Remote-Streaming-Fileread`) — via `scripts/live_solr_discovery.py`, reusing the **same** `EGRESS_FETCH` primitive (no new catalog entry), with **no** carrier (Solr has no host guard) and **no** oracle change (the mount-base fix already fires on `/solr/`). The only new code is a **general** source-ingest generalization (`source_ingest.py`: non-servlet param-bag entrypoints, symbolic-constant param-name resolution, cross-class URL-fetch wrapper sinks — every Solr particular *learned from Solr's own source*, no literal) plus a one-line empty-route join (`hypothesis.py::_target_url`); catalog/intent/reachability/carrier unchanged. Written up in `docs/discovery-engine-slice2-transfer.md`. **Raw-auditable confirmations (§4.6 closure):** a discovery/proof confirmation must be independently re-derivable from the artifact, not self-asserted, so every SSRF confirmation persists a bounded `ConfirmationEvidence` pair (`models/methodology.py`) into **both** the finding evidence and the phase-5 trace: (a) the raw **confirming excerpt** — the reflected bytes anchored on the internal-content marker (GeoServer's own `…<title>GeoServer: Redirecting</title>…` mount-base page, proxied back), and (b) the **control** it was distinguished against — for a carrier-bearing confirm the *same* internal-target probe re-sent WITHOUT the Host carrier (`ExploitAgent._ssrf_internal_confirmation_evidence`), which the servlet rejects (`IllegalStateException`, marker absent), so "the marker is fetched content, not response chrome" is visible in the bytes (the §4.2 carrier isolation is now in the trace, not only the driver's stdout). Bounded (≤360 chars/excerpt); cloud-metadata excerpts redact credential values (field name proves access, never the secret). `ConfirmationEvidence` is the general shape; SSRF is the first consumer. **Second capability class — file read (Flink CVE-2020-17519, live-validated):** the first proof that the capability-**catalog** abstraction holds a class OTHER than SSRF/egress-fetch, reducing to an ALREADY-BUILT oracle. A new `FILE_READ` primitive (`catalog.py::FILE_READ_JAVA_FILE_SINK`) binds `test_method="_test_lfi"` / `confirmation_primitives=["P3"]` — **zero new proof code**; it rides the same `_test_lfi` plan-union and the same P3 file-content oracle the black-box LFI methodology already uses. The engine **extended, did not fork**: `match_primitives`/`compute_delta`/`compute_reachability`/`generate_hypotheses` are keyed on `PrimitiveClass`, unchanged. What the file-read *class* required (the honest metric): (1) the catalog entry; (2) three general Java **source idioms** in `source_ingest.py`, learned from Flink's own source with no Flink literal — a **typed path-param entrypoint** (`getPathParameter(Xxx.class)` → wire name via the class's `KEY` constant; route `/jobmanager/logs/:filename` learned from a `String.format("/…:%s", Xxx.KEY)` builder), a **file-read sink** (`new File(dir, name)` / `new FileInputStream` / `Files.read*` tainted by a request param), and a **basename-strip guard** (`.getName()`/`getCanonicalPath`/`normalize` → SANCTIONED; its absence is the intent-gap = EXPOSED — the missing `.getName()` Flink's fix added; a patched fixture yields **no** hypothesis, N/A by construction); (3) one piece of **oracle-sensitivity work** — a dedicated **path-segment-traversal carrier** (`exploit.py::_path_send_probe`, constraint `CARRIER_PATH_TRAVERSAL`, attached per-instance when the channel is a URL path param, the FILE_READ analogue of GeoServer's Host carrier): the generic path carrier `quote(safe="")`-re-encodes an already-encoded `..%252f` token (defeating the traversal) AND leaves the target's literal `/` (`etc/passwd`) splitting the segment to a different Flink handler, so the carrier substitutes the token verbatim and normalises every literal `/` to the payload's own encoded-slash token so the whole payload stays ONE opaque path segment. File read is `ConfirmationEvidence`'s **second consumer**: `_test_lfi` phase 5 emits the P3 pair (leaked `root:x:0:0:` bytes + a benign non-traversal control that reads nothing, `control_reflected_it=False`), with `confirming_target` set to the reproducible **wire** URL. The file-read class also **transfers to a second codebase for free** — Solr's `stream.file` → `new File(file)` is surfaced by the same catalog entry (a genuine local-file-read vector, additive to the unchanged `stream.url` SSRF). Live-validated end-to-end vs a real Flink 1.11.2 with a LIVE Anthropic exploit LLM by `scripts/live_flink_discovery.py`; written up in `docs/discovery-engine-file-read-class.md`. **Third capability class — Log4Shell log-sink egress (CVE-2021-44228, the flagship, live-validated):** the first class whose proof reduces to the **out-of-band (P6)** primitive rather than an in-band oracle, and the first with **heuristic, cross-function, manifest-gated** reachability. A new `LOG_INTERPOLATION` `PrimitiveClass` + catalog entry (`catalog.py::LOG4J_INTERPOLATION_JNDI`) binds `test_method="_test_log4shell"` / `confirmation_primitives=["P6"]` and matches on a **manifest-derived** token (`constants.py::LOG4J_VULNERABLE_TOKEN`), NOT a bare "java" match — so it is version-gated. What the log-sink *class* required (the honest metric — all general, specifics learned from source): (1) the catalog entry; (2) two general Java **source idioms** in `source_ingest.py`, learned from Solr/log4j's own source with no Solr/CVE literal — a **logging-call sink** (`log(ger)?.(trace|info|warn|error|debug|fatal)(…)` incl. Solr's `log().info(…)` accessor, whose argument is request-derived: a tainted var, a request-param-bag read `req.getParams()`, or a direct accessor → a `LOG_INTERPOLATION` call site, coarse-tainted `*request*` since the whole bag is logged) and a **manifest-capability scan** (`_scan_log4j_manifest`: parse `pom.xml`/gradle/ivy/jar-name for a declared `log4j-core` version; `< 2.15.0` ⇒ emit the vulnerable token, learned from the manifest; a patched line ≥ 2.15 emits nothing ⇒ the primitive is not active ⇒ **N/A by construction**, proven by the `solr_log4shell_patched` fixture) — plus a general broadening of the param-read idioms to the two-arg `.get(NAME, default)` form (Solr's `params.get(ACTION, …)`, so `action` is *learned*, never hardcoded); (3) the first **heuristic reachability** (`reachability.py::_log_sink_edges`): the channel→sink path is NOT intra-function (`action` flows request → dispatch → a logging call deep in log4j-core), and log4j interpolates *any* logged string, so the honest prior is loose — each untrusted entrypoint param (bounded, `action` leads) is wired to a representative log sink at `STATIC_HEURISTIC` grade, ranked below intra-function `STATIC_CONFIRMED`. **Reachability-as-prior, P6 proves** (§4.3): the prior *ranks* candidate channels, the callback *confirms* which reach the logger (the rest defer `blind_unconfirmed`, never false-emit). **Zero new proof code:** `_test_log4shell` selects the CLINKZ-OWNED nonce-only `JNDI_DNS` template and rides the same P6 `_oob_send`→`collab.reap`→`_build_oob_confirmation_evidence` machinery the blind-SSRF path uses (fire-and-reap over the endpoint's channels, one shared window); the JNDI string is just a param VALUE through the shared string carrier (no dedicated carrier). Discovery-driven only (`method_map`, not black-box applicability). **Live-validated end-to-end vs a real Vulhub Solr 8.11.0 + log4j-core 2.14.1 + a live collaborator + a live Anthropic exploit LLM** (`scripts/live_log4shell_p6_validation.py`, engagement `0be7cb63`): 8 CRITICAL Log4Shell findings, each raw-auditable (the outbound `${jndi:dns://…T…}` probe + the inbound DNS callback bearing the same `T` + the never-sent control), confirmed both directly and through the real `run()` seam with the live LLM; exfil guardrail holds; a zero-FP egress-denied probe emits nothing; SSRF (in-band+OOB) and file-read unregressed. Written up in `docs/discovery-engine-log4shell-class.md` with the raw `outputs/p6-log4shell/` artifacts.
- **Capability-catalog learning loop — Layer 2, slice 1 (WRITE side; `docs/discovery-engine-capability-learning-design.md` §S1)**: the discovery catalog's per-technology capability memory — the ONE cross-engagement learning system after the technique-success loop is **retired** (§2.1). Two new tables in `clinkz_knowledge.db` (`persistent_kb.py`): a durable **`capability_facts`** (UPSERTed on `UNIQUE(technology_key, version_predicate, primitive_class, sink_shape_id)`) and an append-only **`capability_observations`** ledger. **No target-identity column** anywhere (no host/URL/IP/port/secret) — the schema-level exfil guardrail (§5.3); `observed_technology` is a fingerprint *string*, a tech not a host. Models `CapabilityFact`/`CapabilityObservation` in `discovery/models.py` (`CapabilityRecall` deferred to slice 2). **Write-back re-points the existing chokepoint** `exploit::_persist_finding → _record_finding_to_kb`: a finding carrying discovery provenance UPSERTs a positive `confirmed` fact + appends a `confirmed` observation whose `evidence_ref` is a **LINK** (`finding:<id>:<Pn>`) into the finding's own `ConfirmationEvidence`, never a bytes copy; non-confirming outcomes (`failed_unreachable`/`blind_unconfirmed`/`collaborator_unavailable`) write an observation-only ledger row and **never** a durable negative fact (§3.5 poisoning-safety), `failed_gated` refines `gating_config` (positive present-but-gated knowledge). `_test_log4shell` self-records the precise `collaborator_unavailable`/`blind_unconfirmed`; a general dispatch-level fallback in `_execute_task` records the honest `failed_unreachable` for any discovery task that neither confirmed nor self-recorded. **Provenance plumbing**: a `DiscoveryProvenance` bundle (in `models/finding.py`, plain-`str` enum values to avoid the discovery↔finding import cycle) on `ExploitTask`/`Finding`/`PageAnalysis`, populated hypothesis → task → finding — `sink_shape_id` (a new tag on each `CallSite` from the recognizer's fixed vocabulary `java.url_openconnection`/`java.file_sink`/`log4j.log_sink`), structured `SourceModel.manifest_technology_key`/`manifest_observed_version`, and the version identity `generate_hypotheses` derives (manifest carrying-dependency `log4j-core`/`2.14.1` for log4j, else a normalized fingerprint). **`confidence`** is a corroboration-with-recency-decay PRIOR computed from **CONFIRMING observations ONLY** (`1 - 0.5^k` distinct confirming engagements × a floored recency decay) — the §2.2↔§3.5 inconsistency resolved so a per-engagement failure can **never** lower a real technology's confidence (the `WHERE outcome='confirmed'` firewall); it is a PRIOR that **never gates emission** (slice 1 does not even read facts back — recall is slice 2). **Emission is unchanged** (§5): Layer 2 holds no path to `_persist_finding`; the emitted finding set is identical whether the fact store is empty or full. Retirement is a clean cutover — `record_technique_result`/`update_success_rates`/`get_past_results_for_technology` remain **read-only** for the report's historical view, `success_rate` frozen (no new writes).
- **Capability-catalog learning loop — Layer 2, slice 2 (READ side; design §2.3/§2.4/§4/§6, live-validated)**: the load-as-prior half — recall re-orders and *completes* which hypotheses are TESTED; it **never emits and never marks a target vulnerable** — emission stays the unchanged P1–P6 proof on the live target (§5), so a full store can only ever *raise* recall, never manufacture a finding or a memory-induced false negative (no negative facts exist). **Deterministic version predicates (`discovery/versions.py::version_satisfies`)**: the grammar `'<X' | '<=X' | '=X' | '[X,Y]' | '*'` (+ `'>X'/'>=X'`) on the shared `parse_version` tuple idiom (`_parse_semver` now delegates to it); a recall fires only when the observed version satisfies the fact's predicate (the primary staleness guard — a patched version fails the predicate → no recall → no budget), and a version we cannot observe is the widest, lowest-confidence match (penalised). **Deterministic transfer-edge writers (`discovery/relations.py`)** — the dormant `technology_relations` table's FIRST production writers: `bundles` (manifest-derived, `apache-solr@8.11.0 → log4j-core@2.14.1`, similarity 1.0 — only a *versioned specific product* bundles the dep, never bare `Java`) and `successor` (version lineage); `similar` (heuristic/LLM) is **deferred**. `normalize_tech_identity` is the ONE identity idiom the writer and reader share so an edge written this engagement matches byte-for-byte next. **`capability_recall` (`discovery/recall.py`) is a PURE READ** over KB-dumped fact + relation rows (no I/O, no write, no emission path): expand the fingerprint over `bundles`/`successor` edges to a carrying dependency the fact keys on, filter by `version_satisfies`, yield `CapabilityRecall(match_kind ∈ {exact_tech,bundles,successor}, match_confidence = fact.confidence × relation similarity, seeds_reachability_grade)`. **Load-as-prior seam** in `DiscoveryEngine.discover` (recalls computed after ingest+fingerprint, before `generate_hypotheses(seeded_by=…)`): **case (a)** a recall matching a cold hypothesis by `(primitive_class, sink_shape_id)` BOOSTS its `rank_score` and KEEPS the earned `STATIC_*` grade (a boost, not a duplicate); **case (b)** a recall whose sink shape the source did NOT re-derive SEEDS hypotheses on the untrusted entrypoint channels at `HYPOTHESIZED` reachability (recall does not fake reachability — the live proof confirms which channels reach the sink), re-keyed on the fact's carrying dependency so a confirmed finding writes back correctly, capped per engagement (§7.3 most-speculative). **Async orchestrator seam**: `_build_discovery_tasks` (now async) dumps `capability_facts` + `technology_relations` as the recall prior, then writes the `bundles`/`successor` transfer edges back for the NEXT engagement (best-effort; a read/write failure degrades to a cold start). **Trace (§6.2 metric)**: `discover` emits per hypothesis `prior_source` (`capability_recall`/`cold_derivation`) + `rank_score` + rank ordinal; `_step_execute_exploits` records each discovery task's actual dispatch ordinal. **Live-validated end-to-end** (the §6 two-engagement experiment, no harness) vs a real Vulhub Solr 8.11.0 + log4j-core 2.14.1 + live collaborator + live Anthropic (`scripts/live_capability_recall_validation.py`, engagements `6b7df62d`/`874f879d`/`50b5dbb3`): `capability_facts`+`technology_relations` 0→1 (the confirmed `log4j-core =2.14.1` fact + the real `bundles` edge); under IDENTICAL partial source (log-sink file withheld) the **cold-control** produces 0 `log_interpolation` hypotheses while the **warm** run recalls via the bundles edge and seeds 3 (`prior_source=capability_recall`), which the **live P6 proof** then confirms (raw-auditable: the outbound `${jndi:dns://…nonce}` probe + the inbound DNS callback bearing the same nonce + the never-sent control) — recall changed the PATH, the proof confirmed the finding. Written up in `docs/discovery-engine-capability-recall-slice2-validation.md`. **Still deferred:** `similar` (heuristic/LLM) transfer edges and the offline catalog-population/CVE-abstraction grower. Cross-service reachability's **B2** (the `reaches` transfer-edge write-back + a SEPARATE topology recall) is now **built** — see the cross-service topology-learning bullet below.
- **Cross-language discovery — slice A1 (JS/TS Node/Express ingestor; deterministic, no live target, no LLM in the ingest path)**: the discovery engine's four downstream layers (catalog → intent → reachability → hypothesis) and the whole Layer-2 loop are keyed on the language-agnostic `SourceModel`, so the ONE language-specific piece is *which ingestor produces it*. That seam is now named as a minimal **`SourceIngestor` protocol** (`discovery/ingestor.py::SourceIngestor`, `ingest_path(root)→SourceModel`) with **`select_ingestor(root)`** picking the implementation deterministically (root build-manifest / `*.java` → `JavaSourceIngestor`; `package.json` / JS-TS sources → `JsSourceIngestor`; Java wins ties; Java default when neither is detected, so an empty/non-source `source_dir` is unchanged; the extension probe skips `node_modules`/build dirs so a JS project vendoring `*.java` never mis-detects). `DiscoveryEngine.discover` re-points its single ingest call to `select_ingestor(source_dir).ingest_path(source_dir)`. The extraction is **pure** — `JavaSourceIngestor` is untouched and satisfies the protocol structurally, so every existing Java fixture (geoserver single-file, solr/flink dirs, with/without a manifest) ingests **byte-identically** through the dispatch (`test_ingestor_dispatch` `model_dump()` equality is the regression gate). **`JsSourceIngestor` (`discovery/js_source_ingest.py`)** is a deliberately-shallow, backend-only MIRROR of the Java ingestor's discipline — regex / bounded-scan-only, source treated as untrusted data, no `eval`, bounded file count/bytes, `node_modules`/build output/`*.min.js`/`*.d.ts` never ingested (Angular/frontend deferred): **entrypoints** = Express `app.`/`router.<verb>(path, handler)` route registrations (taint resolved inside the balanced handler body — the inline-handler shape); **channels** = `req.query`/`body`/`params`/`headers` reads (`req.params` → `path_params`); **call_sites** = JS idioms of the EXISTING primitive classes only — `EGRESS_FETCH` (`axios`/`fetch`/`http(s)`/`got`/`request`/`superagent`, tagged `js.http_egress`) and `FILE_READ` (`fs.readFile*`/`createReadStream`/`res.sendFile`, tagged `js.fs_read`) with a request-tainted argument; **guard** = `path.basename`/`normalize` on the tainted path → SANCTIONED (the file-read analogue of Flink's `.getName()` fix; conservative — over-guard, never a phantom); **manifest** = `package.json` (+ `package-lock`) → carrying-dependency `manifest_technology_key`/`manifest_observed_version` (the HTTP-client lib the egress used, else the backend framework; lockfile version wins), the JS analogue of the `pom.xml` log4j-core scan, emitted on the `SourceModel` for A2's write-back to key on. Taint is one hop (request→local, local/inline read→sink arg); multi-hop reassignment, named-handler indirection, and SSRF host-check guards are documented deferrals. **The ONE Layer-1 touch** (isolated, flagged): `EGRESS_FETCH`/`FILE_READ` are language-AGNOSTIC capabilities (the sink shape differs per language but the Δ-adjudication, reachability and proof oracle are identical), so their `catalog.py` `technology_pattern` (single-sourced `_MULTILANG_FETCH_FILE_TECH`) is widened to match Node/JS as well as Java — no new PrimitiveClass, oracle, or proof-engine change; `LOG_INTERPOLATION` stays Java/log4j-gated. Proven end-to-end deterministically (`test_js_ingest`): the JS `SourceModel` flows `match_primitives → compute_delta → compute_reachability → generate_hypotheses` to the expected `EGRESS_FETCH` (`/fetch`, `url`, `_test_ssrf`, `js.http_egress`) and `FILE_READ` (`/download`, `file`, `_test_lfi`, `js.fs_read`) hypotheses, each lowering to an `ExploitTask` for the unchanged plan-union. First cross-language rung toward a Juice-Shop-class Node target; **out of scope this slice:** live target / exploitation, any recall/write-back/relations change, and Angular/frontend.
- **Cross-language discovery — slice A2b (live generalization on OWASP Juice Shop; real run, no harness)**: proves the discovery engine + JS ingestor GENERALIZE to a real, varied, different-language, non-Vulhub target by surfacing and **live-confirming** one of Juice Shop's OWN-CODE vulns through the JS-source path. **Diagnosis (gated the slice):** the A1 inline-handler ingestor surfaced **0 call_sites** over the real Juice Shop backend (635 files) — 51/53 route handlers use the ESM/TS **factory-per-file** pattern (`export function X () { return (req, res) => {…} }` in a separate `routes/*.ts`, registered wrapped in `server.ts`, e.g. `app.post('/profile/image/url', uploadToMemory.single('file'), utils.asyncHandler(profileImageUrlUpload()))`) that A1's inline-body assumption + `app.use` exclusion missed. **Hardening (minimal, no Layer-1 change):** cross-file **factory-handler resolution** — a second pass (the JS analogue of the Java pass's 2-pass cross-file resolution; no AST/dataflow) indexes every `export function <Name>(...)` body and links a route registration to a referenced factory `name()` across files; `_RE_ROUTE` gains `use` (a bare `app.use(mw)` without a path literal is still never a route). The sink idioms (`fetch`/`fs.readFile`) were already A1-recognized — only the cross-file structure blocked them; deferrals stay shallow (arrow-param-destructured request, bare handler reference, multi-hop/inner-function taint, SSRF host-check guards). Locked deterministically by `test_js_ingest_esm_factory` (a clean minimal reproduction of the ESM/factory shape) + the `js_express_esm_factory` fixture; A1 fixture + Java byte-identical regression unregressed. **Live-validated end-to-end** vs a real Juice Shop **v19.1.1** + live OOB collaborator + live Anthropic exploit LLM (`scripts/live_juiceshop_ssrf_discovery.py`, engagement `cfc21060`): gray-box honesty ingests the RUNNING container's own source (`docker cp`), whose SSRF handler is `fetch(req.body.imageUrl)` @ `routes/profileImageUrlUpload.ts:24`; the JS ingestor surfaces it (`js.http_egress`) → the engine hypothesizes `EGRESS_FETCH` (`POST /profile/image/url`, `imageUrl`, `form_body`) → the **UNCHANGED** proof confirms it via **P6 out-of-band** (the SSRF is blind by design — streams to a file, 302-redirects — and auth-gated, so the driver registers+logs in for a token; the black-box `_test_ssrf` in-band path justifiably emits 0, and the gray-box discovery + P6 primitive is what finds and proves it). **Raw-auditable:** the outbound probe `imageUrl=http://host.docker.internal:18081/<nonce>` + the inbound callback bearing the SAME nonce + the never-sent control; **zero-FP:** auth stripped → the SSRF gate blocks → no callback. `report.json` carries the HIGH `blind_oob_confirmed` finding with JS discovery provenance (`primitive_class=egress_fetch`, `sink_shape_id=js.http_egress`). Written up in `docs/discovery-engine-js-juiceshop-slice-a2b-validation.md` with the raw `outputs/js-juiceshop-a2b/` artifacts. **Out of scope (A2a follow-up):** the carrying-dependency write-back keying (the JS EGRESS hypothesis still keys `technology_key` on the fingerprint `node-js`, not the manifest `express`), JS→JS transfer, and Angular/frontend.
- **Cross-language discovery — slice A2a (carrying-dependency keying + JS→JS transfer; closes cross-language slice A)**: the correctness fix that makes a JS capability fact key on **where the vulnerable code lives**, and the JS edition of the slice-2 "gets smarter" transfer. **The trap A2a fixes:** the A1 model-level `manifest_technology_key` is *the library the egress used* (else the backend framework) — `axios` on the SSRF fixture, `express` on Juice Shop — but those apps call the library from their **own** code (the missing host-allowlist is app code), so keying an EGRESS_FETCH/FILE_READ fact on it would falsely transfer the app's SSRF to every app bundling that library. **Per-sink attribution (the correctness core):** `CallSite.carrying_dependency`/`carrying_version` (`models.py`) are set ONLY when the sink's source `file` lives inside a bundled **declared local-path / workspace dependency** (attributed from the sink's path + that package's own `package.json` — the JS ingestor's `_local_dependency_index`/`_attribute_carrying_dependency`); an **app-code** sink (`routes/`/`src/`/the app root) that merely *calls* a library gets no carrying dependency and stays fingerprint-keyed. `hypothesis.py::_technology_identity` returns the sink's attributed carrying dependency for EGRESS_FETCH/FILE_READ when present, else the unchanged fingerprint; `LOG_INTERPOLATION` path unchanged. Declared local packages are also surfaced as observed technology identities so a package **present-per-manifest but source-withheld** this engagement stays recall-observable — the transfer path rides the **unchanged** recall (`exact_tech` via an observed identity; the unversioned JS fingerprint has no `bundles`-eligible app entry, so no relations change). **Provenance truth fix:** a discovery finding must record the ACTUAL confirming primitive — `_ssrf_phase6_emit` stamps `P6` onto a blind-OOB-confirmed SSRF finding's `discovery_provenance.confirmation_primitive` (via `_stamp_confirming_primitive`, a per-finding copy that never mutates the shared task provenance), overriding the EGRESS_FETCH obligation's *declared* in-band `P3/P1`; the in-band path is unchanged (the `_execute_task` seam stamps the declared value). **Deterministic proof** (`scripts/live_js_transfer_a2a_validation.py` → `outputs/js-a2a-transfer/`, `tests/test_discovery/test_js_transfer.py`): app-A's library-borne sink (`packages/vuln-fetch-lib/index.js`) → fact keyed on the PACKAGE (`vuln-fetch-lib@2.3.1`, a real temp-KB write-back round-trip); cold-control app-B (partial source, EMPTY KB) → 0 egress hypotheses; warm app-B (same partial source, fact recalled) → 1 SEEDED egress hypothesis (`prior_source=capability_recall`, keyed `vuln-fetch-lib`, `HYPOTHESIZED`); **no-over-transfer** — the axios app + the Juice-Shop A2b analogue both stay `node-js`. **Live-confirmed** on real Juice Shop (engagement `c0fe946a`, live collaborator + live Anthropic): the app-code SSRF surfaces keyed `node-js` (not `express`) and the persisted `report.json` finding reads `confirmation_primitive=P6` (not the declared `P3/P1`). Scope fence held: no new PrimitiveClass/oracle, no proof-engine change, no recall/relations logic change; Java/log4j keying unchanged. **Deferred:** the optional language-neutral `primitive_id` rename (JS findings still carry `egress_fetch.java_openconnection`, skipped to avoid churn); `bundles`-edge JS→JS transfer; Angular/frontend. Written up in `docs/discovery-engine-js-a2a-transfer-validation.md`.
- **Cross-service reachability — slice B1 (the reach itself; design `docs/discovery-engine-cross-service-reachability-design.md` §1–§5/§8)**: extends the Δ-model's middle factor (`compute_reachability`) from single-service to *across a service boundary* — the channel enters at service **A**'s entrypoint, A's egress forwards attacker-influenced data across a process boundary, and the sink/impact lives in a *different* in-scope service **B**. **Extends, does not fork:** no new `PrimitiveClass`, no new oracle, no new `_test_*` method, no change to `_persist_finding`. **(1) New grade** `SoundnessGrade.CROSS_SERVICE_TOPOLOGY` (`discovery/models.py`), strictly the WEAKEST member (below `HYPOTHESIZED`), with `reach_confidence` sub-ordering by discovery source (source 0.35 > recon 0.25; catalog 0.15 is B2, now built — see the topology-learning bullet). **(2) Min-over-composition grading** (`compose_soundness`, `_SOUNDNESS_RANK`): a cross-service chain's grade is the MINIMUM over its hops, implemented explicitly (return the highest-rank == weakest grade) so it is *structurally impossible* to grade a chain by its strongest hop — a `STATIC_CONFIRMED` A-side egress composed with the `CROSS_SERVICE_TOPOLOGY` boundary hop grades `CROSS_SERVICE_TOPOLOGY`, never `STATIC_CONFIRMED`. Because edge₂ (A→B) is always the weakest hop, **every cross-service chain grades `CROSS_SERVICE_TOPOLOGY`** and can never out-rank a single-service hypothesis. **(3) Deterministic topology discovery** (`discovery/topology.py`, §2 a+b only): **SOURCE** — A's source statically constructs a URL whose host resolves to an in-scope B (`scan_static_egress_hosts` — a bounded, self-contained URL-literal scan that touches no existing recognizer; a literal becomes a SOURCE edge ONLY when it matches an in-scope B, so an incidental namespace/docs literal is inert), the config/const target that distinguishes "A intentionally calls B" from "A merely has an SSRF"; **RECON** — B is an in-scope service reachable from A's segment (network adjacency, weaker — the wire is open, no proven call). `discover_cross_service_edges` composes A's `EGRESS_FETCH` edges (edge₁) with the A→B boundary hop per in-scope B candidate (bounded), emitting `CROSS_SERVICE_TOPOLOGY` edges carrying `cross_service_target` (B's URL) + `topology_source`. **General shapes only — no host/IP/URL literal in any logic or persisted schema** (identities come from the runtime `EngagementScope`/recon and the TARGET's own source, untrusted data). Wired into `DiscoveryEngine.discover(topology_context=…)` (a `TopologyContext` the Orchestrator builds from scope: A's origin + the in-scope B set); **additive** — with no context the edge set is byte-identical to single-service, and no single-service hypothesis's grade or rank changes (locked by `test_cross_service_topology`). **(4) Cross-service SSRF hypothesis** REUSES `EGRESS_FETCH` — an `EGRESS_FETCH` Δ whose reaching edge carries `CROSS_SERVICE_TOPOLOGY` and whose confirmation target is B's internal URL; it lowers to a `_test_ssrf` `ExploitTask` carrying `cross_service_target`/`cross_service_source`. `ExploitAgent._execute_task` routes a task with `cross_service_target` set to a dedicated `_confirm_cross_service_reach` driver (the single-service `_test_ssrf` path is untouched). **The confirmation reduces to the UNCHANGED P6 machinery** (`_oob_send` → `collab.reap` → `_build_oob_confirmation_evidence`) — zero new proof code. **The honesty crux (§3 rung-3 — the whole slice's test), the co-location gate:** a cross-service *finding* is emitted ONLY when the confirming callback's destination is **co-located with B** (`EngagementScope.addresses_equivalent(collab.callback_zone(), B)` + `scope.contains(B)`). A callback at a **generic** collaborator NOT co-located with B proves only "A egresses *somewhere*" (already plain SSRF) — conflating that with "A reaches B" is the cross-service phantom this slice forbids, so it becomes a **research-lead** (`egress_confirmed_but_B_reach_not_observed`), never a finding. A confirmed cross-service reach is a normal SSRF `Finding` (severity high, provenance stamped `P6`) whose EGRESS_FETCH capability fact writes back via the **unchanged** `_persist_finding` chokepoint (no `reaches` KB edge — that is B2). **(5) `CrossServiceResearchLead`** (`models/finding.py`) is a NEW type that is **NOT a `Finding`** and holds **no path to `_persist_finding`**: it carries `candidate_chain` + per-hop provenance + `why_unconfirmed` (closed vocab `CROSS_SERVICE_WHY_UNCONFIRMED`: `egress_confirmed_but_B_reach_not_observed` / `B_not_instrumentable` / `blind_unconfirmed_within_window` / `topology_prior_only`) + the raw null result (the exact probe sent + the null observation), bounded per engagement (`_MAX_CROSS_SERVICE_RESEARCH_LEADS`). Persisted to its OWN state-store table (`research_leads`, separate from `findings`), read into `PentestReport.research_leads` (a separate field), and rendered in a DEDICATED report section *"Cross-service research leads (candidate chains — UNCONFIRMED)"* — **never counted in coverage, never rendered as a confirmed finding** (a type-system + storage-level property, not a convention). **Zero-FP invariant (§4/§7):** cross-service *reasoning* (topology discovery, grading) only ever produces or re-orders candidate chains + research-leads; emission stays the unchanged P6 oracle firing at a collaborator co-located with B — a wrong A→B edge cannot forge the nonce callback, so a topology prior can only change the tested set / its order / the research-lead list, never manufacture a finding. **Scope fence held:** no new `PrimitiveClass`/oracle, no `reaches` KB write-back or recall expansion (B2), no general multi-hop chain solver, no sequence/stateful reachability, P1–P6 untouched. **Live-validated end-to-end** (the §8 two-service experiment, no harness) vs live Juice Shop (service A — its own-code `fetch(req.body.imageUrl)` SSRF, JS-source-discovered) + a live OOB collaborator mounted at B's internal address + a live Anthropic exploit LLM (`scripts/live_cross_service_ssrf_validation.py`, engagements `568c6d83` arm b / `81825e38` arm c): **(a)** the A→B edge is discovered `grade=cross_service_topology source=recon` (edge dump); **(b) arm b** (collaborator co-located with B) → a HIGH cross-service SSRF finding, raw-auditable (the outbound probe carrying nonce `T` targeting B + the inbound callback bearing the SAME `T` recorded AT B + the never-sent control, `provenance.confirmation_primitive=P6`); **(c) arm c — THE PHANTOM CONTROL** (the SAME chain with a GENERIC collaborator NOT co-located with B, the ONLY variable changed) → a callback still lands (A egresses) but the co-location gate refuses it → **0 cross-service findings + a research-lead** `egress_confirmed_but_B_reach_not_observed`, proving the slice will not conflate "A egresses" with "A reaches B". Written up in `docs/discovery-engine-cross-service-reachability-b1-validation.md` with the raw `outputs/cross-service-b1/` artifacts.
- **Cross-service reachability — slice B2 (topology LEARNING, the loop half; design `docs/discovery-engine-cross-service-reachability-design.md` §6/§6.4/§7)**: B1 built the reach; B2 makes it **compound across engagements**. A CONFIRMED A→B reach writes an abstracted `reaches` topology edge that a later engagement recalls to seed a chain its cold source could not derive. **Extends, does not fork:** no new `PrimitiveClass`/oracle/`_test_*`, no change to `_persist_finding`, and — critically — **no widening of the capability-transfer allowlist**. **(1) `reaches` relation kind + the §6.4 ABSTRACTION FENCE at the WRITE boundary (the load-bearing guarantee — the A2a no-over-transfer equivalent):** `relations.py::derive_reaches_edge` builds an edge ONLY when BOTH ends abstract to a role/tech-class via `abstract_reaches_identity` (a SERVICE↔SERVICE edge, unlike bundles/successor's TECH↔TECH). The fence **refuses** any host/IP/URL/`:port` shape (precise detection — it still accepts a recon product name with a version dot like `Apache Solr 8.11.0` → `apache-solr`), any bare-language/runtime token (`java`/`node-js` — the same over-broad class `derive_bundles_edges` refuses for an unversioned app), empty, and a self-edge. **No host/IP/URL literal in the fence logic or any persisted row.** If either end is un-abstractable, the topology stays **ENGAGEMENT-LOCAL** — the confirmed finding still stands, but nothing deployment-specific enters the cross-engagement KB. **(2) YES-only write-back** (`exploit.py::_write_reaches_edge`, mirrors the capability loop): the write lives on the CONFIRMED branch of `_confirm_cross_service_reach`, AFTER the finding is emitted — a research-lead / unconfirmed / refused reach `return`s before reaching it, so it is *structurally* impossible for a non-confirmed reach to write a durable edge. A's identity is the **specific** service role (`owasp-juice-shop`, threaded via `TopologyContext.origin_identity` → the edge/task's `cross_service_a_identity`), deliberately distinct from the capability fact's key (which may be a broad app-code `node-js`); B's is `cross_service_b_identity` (recon-derived, NEVER B's URL). **(3) SEPARATE topology recall** (`discovery/topology_recall.py::recall_cross_service_edges`, a pure READ): reads ONLY `relation_type=='reaches'` rows — it does **NOT** add `reaches` to `recall.py::_reachable_keys`'s allowlist (which stays closed to bundles/successor), so **capability transfer and topology transfer are different knowledge kinds that never cross** (a `reaches` edge can never transfer a capability fact — locked by `test_reaches_edge_cannot_transfer_a_capability_fact`). **(4) Load-as-prior:** a recalled `reaches` prior whose A-end matches this engagement's `origin_identity` and whose B-end matches an in-scope B candidate's identity seeds a cross-service edge at `CROSS_SERVICE_TOPOLOGY` grade, `topology_source='catalog'`, `reach_confidence=0.15` (strictly **below** recon 0.25 — a learned topology is the weakest discovery source), composed via the shared `build_cross_service_edge`; only for B targets the deterministic recon/source pass did not already cover (`engine._append_cross_service_edges`, additive). **Still fully proof-gated:** emission remains B1's unchanged co-location + P6 gate — a stale/wrong learned edge only wastes a probe or surfaces a research-lead, never a finding (§7 zero-FP, structural). **Orchestrator seam:** `_build_topology_context(base_url, technologies)` sets `origin_identity` from A's most-specific abstractable fingerprint entry (`_abstract_origin_identity`); `service_identities` is left empty by the black-box multi-target orchestrator (B typically un-abstractable ⇒ engagement-local, the honest §9 default), supplied directly by a gray-box driver that knows B's role. **Deterministically proven** (keyless, `tests/test_discovery/test_cross_service_learning.py` + `tests/test_agents/test_cross_service_learning_writeback.py`): the fence (host/IP/bare-lang refused, role pair accepted), no-cross-contamination, the catalog seed at 0.15, and a two-engagement KB round-trip where the SAME withheld-adjacency source yields a cold-control 0 vs a warm 1 catalog-seeded cross-service hypothesis. **Live two-engagement validation** driver `scripts/live_cross_service_topology_learning_validation.py` (Juice Shop A → internal B, live collaborator + live Anthropic, artifacts to `outputs/cross-service-b2/`). **Scope fence held:** no new `PrimitiveClass`/oracle, no capability-allowlist widening, no `similar` edges, no multi-hop chain solver, no sequence/stateful reachability, P1–P6 + B1's co-location gate untouched.
- **Dispatch (round-robin, no phase deadline by default)**: `_step_execute_exploits` groups planned tasks by vuln-class and dispatches one per class in rotation — every category runs its first task before any category runs its second. By default there is **no exploit phase budget** (`EXPLOIT_PHASE_BUDGET=0`): the full task queue runs to completion and no category is starved by a phase-level clock — operation-level timeouts (per HTTP request, per tool subprocess, per LLM call) are the safety valve. A category is still moved to the back of the rotation once it crosses a soft cap (`exploit_category_max_findings`, default 5 findings, OR `exploit_category_time_budget`, default 90s) — but with no phase budget this only affects ordering/fairness; every task still runs. Set `EXPLOIT_PHASE_BUDGET>0` to restore the old cooperative-stop behaviour (the agent stops dispatching new tasks shortly before the budget elapses and returns cleanly).
- **Verification-honest emission**: A methodology may only emit a finding when its own evidence confirms exploitability. IDOR gates the phase-3/4 LLM checkpoints behind a deterministic divergence check (a probe must differ from baseline in status, length, or normalised body fingerprint) — identical responses make no LLM call and emit nothing. Five further IDOR guards keep that honest: (1) auth-form / credential / CSRF-token params (`username`, `password`, `Login`/`submit`, `user_token`, and any `csrf`/`token`/`nonce`/`captcha` name) are **not object references** and are excluded from candidacy at the top of phase 1 before any probe; (2) the IDOR divergence fingerprint (`_idor_body_fingerprint`) folds long hex runs in addition to numbers/whitespace, so a page that regenerates a per-request CSRF token (e.g. DVWA `login.php`, identical length 1524→1524 but a fresh `user_token` each GET) does NOT read as divergence — closing the phantom where login-form params emitted IDOR findings; (3) **reflection-sink exclusion** — phase 1 sends a canary probe and, if the canary is echoed AND substituting the original value back where it reflected reconstructs the baseline fingerprint, the param is a reflection sink (e.g. DVWA `xss_r`'s `name` → "Hello &lt;value&gt;") and is excluded before any LLM call (a param echoing the value you sent is reflection, never another principal's resource); the phase-5 echo branch that read a reflected reference as "new identifier echoed" is removed; (4) **phase-5 resource honesty** — a verified IDOR's response must be another principal's actual record, so phase 5 rejects error/not-found/denied pages and responses that collapse to a fraction of a substantial baseline (the `view_source_all.php?id=<garbage>` 33484→1730 phantom); (5) **phase-5 authz-boundary precondition** — an IDOR is the bypass of an authorization *boundary*, so phase 5 is passed the phase-2 `primitives` and refuses to verify when `authz_check_present` is False: if phase 2's out-of-allotment probe (`id=99999999` / all-zero UUID) was served a normal 2xx instead of 401/403/redirect-to-login, the endpoint is a boundary-less public lookup and content that merely differs between `id=2` and `id=3` is it doing its job, not an authz break (engagement ec39350b confirmed `info.php?id=2→3` despite `id=99999999`→200 — a 200-on-garbage endpoint with no control to cross); a genuine IDOR whose out-of-range id IS rejected still verifies. Probe URLs are built so a same-named query param is **replaced in place** (`_build_request_url`), never appended as a duplicate (`?id=a&id=b`, whose effective value is server-dependent). `_build_request_url` also resolves `:id`/`{id}` **path-segment placeholders** first (`_resolve_path_params`) from the task params, or a benign default (`1`) so the base fetch never requests a literal `:id`, so discovered templated SPA/API routes like `/rest/basket/:id` are probed by substituting into the path (the real IDOR probe `/rest/basket/2`) rather than appending a stray `?id=`; query params are still appended/replaced as before. Stored XSS emits only when phase-5 verification confirms the payload reflects unescaped in an executable position; a synthesized-but-unverified payload emits nothing.
- **Report-integrity fixes (DVWA `f7a761a1`)**: four correctness/honesty fixes so every confirmed finding has real, correctly-classified, non-duplicated evidence. **(BUG 1) Reflected-XSS verifying-response capture**: `_run_xss_reflected_methodology` threads the phase-5 verifying response (anchored on the reflected payload, `_xss_evidence_snippet`) onto `MethodologyResult.verifying_response`, and `_xss_phase6_emit` renders THAT as the Response evidence — never the old hardcoded `(response unavailable)` placeholder. **Honesty guard**: `verified` but no captured response ⇒ emit returns `None` (verification ran blind — never a silently-verified finding), the same "reject on missing evidence, not on a label" discipline as CMDi/LFI phase-5; this stopped the FP-suppression pass from flagging a genuine Low reflected XSS. **(BUG 2)** SSRF `file_scheme` removed — `file://` is LFI (see the SSRF bullet). **(BUG 3)** CSRF security-sensitivity gate (see the CSRF note above). **(BUG 4) Finding-level dedup**: `_persist_finding` is the chokepoint — it keys each finding `(_canonical_target, title)` in `_emitted_finding_keys`, persists each `(vuln-class, endpoint, param)` once, and callers add to the in-memory result only when it returns `True`. `_fetch_page` strips a bare/empty trailing `#` (`_strip_empty_fragment`, keeping meaningful SPA fragments) so `/sqli/` and `/sqli/#` are one endpoint and the `#` never leaks into titles/evidence; `_canonical_target` (`urlparse`→`urlunparse` + host-case fold) collapses any residual variant. `_make_finding` is a second bare-`#` chokepoint — it strips the empty fragment from the finding's *target* (`_strip_empty_fragment`, URL-aware) and from a *title* that embeds the endpoint URL (`_strip_empty_fragment_token`, text-aware — the CSRF / weak-session / captcha titles), so the artifact never leaks into the rendered report even when the finding's URL did not come from `_fetch_page`. The security-headers tracker now keys `(origin, header.lower())` so the LLM's inconsistent header-name casing across pages no longer emits the same origin's header twice. Distinct endpoints / params / headers stay distinct.
- **Post-run false-positive marking (Step 3b)**: `_llm_analyze_results` names suspected false positives in `ExploitAnalysis.false_positive_suspects` (typed `list[dict]` — `{id, reason}`). **Identity is code-owned**: the analysis prompt addresses each finding by a 1-based **`index`** the code assigns (never a uuid), and `_resolve_fp_suspects` maps the ordinal back to the real id — a finding id the LLM free-writes is ignored, so a mis-copied uuid can no longer bind a rationale to the wrong finding (the a5800a26 mis-bind, which once suppressed a genuine blind SQLi while leaving a junk missing-HSTS finding confirmed HIGH, 2f39bbec). `_mark_false_positive_suspects` then applies two trust gates before suppressing: a **cross-check** (`_fp_reason_matches_finding` / `_classify_fp_topic` over `_FP_TOPIC_SIGNALS`) rejects a mark whose rationale classifies to a *different* vuln class than the bound finding (an HSTS rationale on a SQLi id param — logged for the operator, finding left CONFIRMED); and a **corroboration gate** (`_fp_has_deterministic_corroboration`) only flips a phase-5-verified (CONFIRMED) finding to `FALSE_POSITIVE` when a deterministic signal backs the suspicion (its own captured response was a 4xx/5xx — the reflection-in-error-page shape), otherwise it **annotates** the finding `[SUSPECTED FALSE POSITIVE — operator review]` and keeps it. Marked / annotated findings are never removed — the operator still adjudicates them in the report; verification-honest emission remains the primary phantom guard, this is an advisory layer on top. The analysis prompt explicitly names the **reflection-in-error-page** false-positive class — a marker/canary/value observed only in a 4xx/5xx or stack-trace response, surfaced via the `(status=…)` threaded into each finding's Response evidence — so a CMDi-shaped phantom that ever slips past phase-5 is also catchable here (it passes both gates: topic match + error-status corroboration). That closes the a07df54b gap where this pass flagged the XSS phantoms (their evidence narrated the rejection) but missed the CMDi ones (whose evidence read like success, status unrecorded).
- **Tier 2/3**: `_test_tier2_technique` / `_test_tier3_technique` consume Research Agent runbook entries

### Critic Agent
- **Role**: Quality assurance — validates findings before they enter the report
- **Reviews**: CVSS scoring accuracy, false positive elimination, evidence completeness, reproduction steps
- **Can reject findings**: Sends them back to Exploit Agent for re-validation via Orchestrator

### Report Agent
- **LLM**: Zero LLM calls today — pulls findings from the state store and emits structured JSON + a Markdown summary in <30 s. (The LLM-driven multi-pass narrative described in earlier plans is on the W3 horizon.)

## Tool Execution: Dynamic Discovery

### How Agents Find and Use Tools
Agents do NOT have hardcoded tool lists. When an agent needs to perform an action:

1. **LLM Reasoning**: The agent's LLM decides what capability it needs (e.g., "I need to scan ports on this host")
2. **Tool Research**: The agent checks what's available:
   a. Query the Tool Resolver for locally installed tools matching the need
   b. Check for available MCP servers that provide the capability
   c. If nothing found, use LLM web search to research what tool would work and how to use it
3. **Execution**:
   a. If MCP server available → connect as MCP client and call the tool
   b. If local CLI tool available → execute via subprocess, parse output using existing parsers
   c. If neither → agent reports to Orchestrator that it lacks the capability

### Tool Resolver (src/clinkz/tools/resolver.py)
Central component that agents query to find tools:
- Maintains a registry of locally installed tools (our existing ToolBase wrappers)
- Discovers running MCP servers on known endpoints
- Returns tool availability and connection method (mcp / local / unavailable)
- Agents call resolver.find_tool(capability="port_scanning") not resolver.get("nmap")

### Existing Tool Wrappers
The existing ToolBase parsers (nmap, subfinder, httpx, etc.) serve as the local execution backend. They are NOT thrown away — they become one execution path that the Tool Resolver can offer.

## Tech Stack
- Python 3.12+ with asyncio for concurrency
- **LLM-agnostic design** — all LLM calls go through `src/clinkz/llm/base.py`
- Supported LLM backends:
  1. Anthropic API (Claude Sonnet / Opus) — primary for the Exploit agent (pinned)
  2. Google Gemini API — primary for Recon / Scan / Report (`gemini_model`, default `gemini-3.1-flash-lite` GA) and Research (`gemini_research_model`, default `gemini-3.1-flash-lite` GA, with native Search Grounding). Never the shut-down `-preview` variant. Per-client RPM ceiling via `GEMINI_MAX_RPM`.
  3. OpenAI API (GPT-4o / GPT-4o-mini) — third in the resilient fallback chain
  4. Ollama (local models) — stub, not yet wired into the fallback chain
- LLM provider is set via config: `LLM_PROVIDER=openai` / `anthropic` / `gemini` / `ollama`
- Per-agent provider overrides via `LLM_PROVIDER_<AGENT>` (e.g. `LLM_PROVIDER_EXPLOIT=anthropic`)
- Resilient client (`llm/fallback.py`) automatically rotates providers on rate-limit/timeout
- MCP Python SDK (`mcp[cli]`) for tool server/client protocol
- SQLite — `clinkz.db` for per-engagement state, `clinkz_knowledge.db` for the cross-engagement persistent KB (incl. the Layer-2 capability memory: `capability_facts` + `capability_observations`)
- Docker for sandboxed tool execution (`clinkz-tools` container; preflight in `tools/docker_preflight.py`)
- Report agent emits JSON + Markdown directly (no Jinja/WeasyPrint pipeline today)
- Typer for CLI interface; `clinkz trace inspect <engagement>` renders execution traces

## Project Structure
```
clinkz/
├── CLAUDE.md
├── CLINKZ_V2_IMPLEMENTATION.md
├── README.md
├── pyproject.toml
├── src/
│   ├── clinkz/
│   │   ├── __init__.py
│   │   ├── __main__.py             # python -m clinkz entry point
│   │   ├── cli.py                  # Typer CLI (scan / recon / crawl / exploit / report / trace)
│   │   ├── config.py               # Settings (env vars, per-agent LLM overrides)
│   │   ├── state.py                # Engagement state store + message store (SQLite); findings + research_leads (cross-service B1, separate table) + endpoints/runbook/sessions
│   │   │
│   │   ├── orchestrator/
│   │   │   ├── __init__.py
│   │   │   ├── orchestrator.py     # OrchestratorAgent — sequential Recon → concurrent (Scan + Research + Exploit) → Report
│   │   │   ├── lifecycle.py        # AgentLifecycleManager (spin up/down, re-spin tracking)
│   │   │   └── prompts/orchestrator_system.md
│   │   │
│   │   ├── agents/
│   │   │   ├── __init__.py
│   │   │   ├── _methodology_helpers.py  # Shared CMDI/LFI phase-1 probe helpers (BaselineProbe, divergence)
│   │   │   ├── _route_discovery.py # SPA/API route discoverers (static JS bundle + OpenAPI) behind RouteDiscoverer seam
│   │   │   ├── _url_safety.py      # is_state_changing_url — crawl/probe guard against session-poisoning links
│   │   │   ├── base.py             # BaseAgent — message bus integration + LLM hookup
│   │   │   ├── recon.py            # Recon v2 — deterministic steps + LLM checkpoints
│   │   │   ├── scan.py             # Scan v2  — service-specific methods + fallback chains
│   │   │   ├── crawl.py            # Crawl agent stub (TODO — currently inert)
│   │   │   ├── exploit.py          # Exploit v2 — LLM plans, deterministic _test_* execute, adaptive XSS/SQLi methodologies
│   │   │   ├── research.py         # Research v2 — persistent KB + cross-engagement learning
│   │   │   ├── critic.py           # Finding validation
│   │   │   ├── report.py           # Report agent — emits JSON + Markdown (zero LLM)
│   │   │   └── prompts/            # System prompts (recon, scan, exploit, research, critic, report)
│   │   │
│   │   ├── comms/
│   │   │   ├── __init__.py
│   │   │   ├── message.py          # AgentMessage model + message types
│   │   │   ├── bus.py              # Async message bus (Orchestrator-mediated)
│   │   │   └── protocol.py         # Communication protocol constants
│   │   │
│   │   ├── credentials/
│   │   │   └── store.py            # CredentialStore + default credential database
│   │   │
│   │   ├── discovery/              # Gray-box discovery engine (Δ-capability × reachability × provable-impact)
│   │   │   ├── __init__.py         # Public exports (DiscoveryEngine, models)
│   │   │   ├── constants.py        # Carrier-constraint identifiers (CARRIER_ALIGN_HOST egress, CARRIER_PATH_TRAVERSAL file-read)
│   │   │   ├── models.py           # SourceModel(+manifest_technology_key/version), Entrypoint(+path_params)/CallSite(+sink_shape_id, +carrying_dependency/version per-sink attribution A2a)/Guard, Capability{Primitive,Delta}, ReachabilityEdge(+cross_service_target/topology_source B1, +cross_service_a_identity/b_identity abstracted role B2), SoundnessGrade(+CROSS_SERVICE_TOPOLOGY)+compose_soundness (min-over-hops B1), ProofObligation, DiscoveryHypothesis(+technology_key/observed_version/prior_source); Layer-2 CapabilityFact/CapabilityObservation/CapabilityRecall
│   │   │   ├── topology.py         # Cross-service A→B boundary hop (slice B1+B2): TopologyContext(+origin_identity/service_identities B2) + discover_cross_service_edges (compose A's EGRESS_FETCH edges with the boundary hop, grade CROSS_SERVICE_TOPOLOGY via compose_soundness; SOURCE 0.35 static-egress-host / RECON 0.25 adjacency / CATALOG 0.15 learned B2) + build_cross_service_edge (shared edge builder) + scan_static_egress_hosts + topology_reach_confidence. No host/IP/URL literal in logic
│   │   │   ├── topology_recall.py  # recall_cross_service_edges (slice B2) — PURE READ over KB 'reaches' rows ONLY (a SEPARATE read from capability_recall; deliberately NOT in _reachable_keys): a learned reaches whose A-end matches origin_identity + B-end matches an in-scope B seeds a CATALOG cross-service edge (0.15, weakest). Never emits — the co-location+P6 gate proves (§7)
│   │   │   ├── versions.py         # version_satisfies predicate matcher ('<X'|'<=X'|'=X'|'[X,Y]'|'*') + parse_version/predicate_point_version — the shared tuple idiom source_ingest delegates to (Layer-2 recall READ)
│   │   │   ├── relations.py        # Deterministic technology_relations transfer-edge writers: bundles (manifest app→carrying-dep) + successor (version lineage) + reaches (slice B2 — SERVICE↔SERVICE topology, via derive_reaches_edge gated by the abstract_reaches_identity §6.4 fence: BOTH ends a role/tech-class, host/IP/URL/bare-language refused → engagement-local); normalize_tech_identity shared idiom ('similar' deferred)
│   │   │   ├── recall.py           # capability_recall — PURE READ over KB-dumped facts+relations: expand fingerprint over transfer edges (_reachable_keys allowlist = bundles/successor ONLY, closed — a 'reaches' edge is skipped so it can never transfer a capability fact), filter by version_satisfies → CapabilityRecall priors (never emits, §5)
│   │   │   ├── ingestor.py         # SourceIngestor protocol (ingest_path→SourceModel) + select_ingestor — deterministic per-tree language dispatch (Java build-manifest/*.java → JavaSourceIngestor; package.json/JS-TS → JsSourceIngestor; Java wins ties, Java default). The ONE language-specific seam; layers above are SourceModel-keyed (slice A1)
│   │   │   ├── source_ingest.py    # JavaSourceIngestor — bounded regex Java source→SourceModel (egress + file-read + log-sink idioms, typed path-param channel, two-arg .get() reads, manifest log4j-version scan → structured technology_key/version, sink_shape_id tags, intra-function taint, guard adjudication)
│   │   │   ├── js_source_ingest.py # JsSourceIngestor — bounded regex JS/TS (Node/Express backend) source→SourceModel (slice A1 + A2b cross-file resolution + A2a per-sink attribution): Express app./router.<verb> AND app.use route entrypoints; two-pass cross-file factory-handler resolution (export function make(){return (req,res)=>{}} in a separate routes file, registered wrapped in server.ts — the real-world Juice Shop shape); req.query/body/params/headers channels; EGRESS_FETCH (axios/fetch/http(s)/got/request → js.http_egress) + FILE_READ (fs.read*/createReadStream/res.sendFile → js.fs_read) sink shapes; path.basename/normalize guard; package.json(+lock) carrying-dep manifest; A2a: per-sink carrying_dependency attribution (a sink under a declared local-path/workspace dependency dir is library-borne, else app-code → fingerprint-keyed) + declared local packages surfaced as observed identities. Backend-only (no node_modules/frontend), one-hop taint, no eval
│   │   │   ├── catalog.py          # Class-generic capability catalog (EGRESS_FETCH openConnection → _test_ssrf/P1,P3; FILE_READ new File(path) → _test_lfi/P3; LOG_INTERPOLATION log4j log-sink → _test_log4shell/P6, manifest-version-gated) + matcher. EGRESS_FETCH/FILE_READ technology_pattern is language-agnostic (matches Node/JS as well as Java, slice A1); LOG_INTERPOLATION stays Java/log4j-gated
│   │   │   ├── intent.py           # compute_delta — Δ adjudication (bypassable host-check = EXPOSED, not a real guard)
│   │   │   ├── reachability.py     # compute_reachability — channel→sink taint edges (intra-function STATIC_CONFIRMED; heuristic cross-function STATIC_HEURISTIC log-sink edges for Log4Shell)
│   │   │   ├── hypothesis.py       # generate_hypotheses(seeded_by=recalls) — Δ×reachability→Tier-A proof jobs; attaches per-instance carrier constraints; _technology_identity keys EGRESS_FETCH/FILE_READ on the sink's attributed carrying dependency when library-borne, else fingerprint (A2a); Layer-2 recall boost (case a) + seed (case b)
│   │   │   └── engine.py           # DiscoveryEngine.discover(topology_context=…) — select_ingestor picks the per-tree ingestor, then co-locates the four layers + the Layer-2 recall load-as-prior + the cross-service edge composition (_append_cross_service_edges, additive — B1 deterministic recon/source edges THEN the B2 catalog topology_recall over KB 'reaches' rows, seeding only uncovered B; none when no topology_context) into one deterministic pass; emits the §6.2 hypothesis trace
│   │   │
│   │   ├── knowledge/
│   │   │   ├── __init__.py
│   │   │   ├── query.py            # KnowledgeBase — MITRE ATT&CK + OWASP WSTG/API/LLM queries
│   │   │   ├── persistent_kb.py    # PersistentKnowledgeBase — clinkz_knowledge.db (cross-engagement); Layer-2 capability_facts + capability_observations write-back + technology_relations transfer edges (get_technology_relations reader for recall) (technique-success loop retired)
│   │   │   ├── seed_playbook.py    # Tier 1 universal-test seeder
│   │   │   ├── payload_loader.py   # Payload list loader
│   │   │   ├── skills_loader.py    # OWASP WSTG skill snippet loader
│   │   │   ├── mitre_attack.json   # MITRE ATT&CK enterprise dataset
│   │   │   ├── owasp_wstg.json     # OWASP WSTG v4.2 dataset
│   │   │   ├── owasp_api.json      # OWASP API Top 10 (2023)
│   │   │   ├── owasp_llm.json      # OWASP LLM Top 10
│   │   │   ├── payloads.json       # Curated payload lists per vuln class
│   │   │   └── skills/             # Reusable skill snippets (xss_*, sqli_*, wstg_*, ...)
│   │   │
│   │   ├── llm/
│   │   │   ├── __init__.py
│   │   │   ├── base.py             # Abstract LLMClient interface
│   │   │   ├── factory.py          # Returns correct client per provider/agent
│   │   │   ├── fallback.py         # ResilientLLMClient — per-agent fallback chains
│   │   │   ├── openai_client.py    # OpenAI GPT-4o / GPT-4o-mini
│   │   │   ├── anthropic_client.py # Claude Sonnet / Opus
│   │   │   ├── gemini_client.py    # Gemini Flash / Pro
│   │   │   └── ollama_client.py    # Stub (not yet in fallback chain)
│   │   │
│   │   ├── tools/
│   │   │   ├── __init__.py
│   │   │   ├── base.py             # ToolBase ABC + ToolOutput
│   │   │   ├── resolver.py         # ToolResolver — capability lookup + ranked fallback chains
│   │   │   ├── mcp_client.py       # MCP client (stdio + HTTP/SSE)
│   │   │   ├── installer.py        # Binary availability checker
│   │   │   ├── binary_identity.py  # Verifies host binary identity (catches namesake imposters)
│   │   │   ├── docker_preflight.py # Ensures clinkz-tools container is up before tool execution
│   │   │   ├── http_client.py      # Built-in HTTP client tool
│   │   │   ├── auth.py             # WebAuthenticator — default-cred testing (cookie/form + JSON/bearer API auth)
│   │   │   ├── nmap.py / subfinder.py / httpx_tool.py / whatweb.py / wafw00f.py
│   │   │   ├── katana.py / ffuf.py / nuclei.py / nikto.py / sqlmap.py
│   │   │
│   │   ├── research/
│   │   │   └── runtime_research.py # Live web search for CVEs, exploits, writeups
│   │   │
│   │   ├── observability/
│   │   │   └── trace.py            # Per-engagement JSONL execution trace
│   │   │
│   │   ├── oob/                    # Out-of-band (P6) confirmation infrastructure
│   │   │   ├── templates.py       # CLINKZ-OWNED payload templates + carrier (BLIND_SSRF_URL / JNDI_LDAP / JNDI_DNS / DNS_LOOKUP; structural exfil guardrail)
│   │   │   └── collaborator.py    # OOBCollaborator — receive-only DNS+HTTP listener, mint/correlate/reap, health-check, dns_authority (jndi:dns:// target)
│   │   │
│   │   └── models/
│   │       ├── scope.py            # EngagementScope, ScopeEntry, ScopeType
│   │       ├── target.py           # Host / Service models
│   │       ├── recon.py            # Recon v2 result models
│   │       ├── scan.py             # Scan v2 result models (HTTP/FTP/SSH/SMB/DB)
│   │       ├── methodology.py      # Adaptive XSS/SQLi intermediate-result models
│   │       ├── research.py         # Research v2 result models
│   │       ├── finding.py          # Finding, ExploitPlan, ExploitTask(+prior_source/rank_score, +cross_service_target/source B1, +cross_service_a_identity/b_identity abstracted role for the B2 reaches write-back — never a URL), ExploitResult(+research_leads), DiscoveryProvenance (Layer-2 capability provenance bundle), CrossServiceResearchLead (B1 — NOT a Finding, no _persist_finding path) + CROSS_SERVICE_WHY_UNCONFIRMED vocab
│   │       └── report.py           # PentestReport(+research_leads), ExecutiveSummary
│   │
├── docker/
│   ├── Dockerfile.tools
│   ├── Dockerfile.dvwa
│   ├── docker-compose.yml
│   └── dvwa-init.sh
├── scripts/                        # Demo / live integration helpers
│   ├── live_full_pipeline.py
│   ├── live_geoserver_discovery.py # Live validation of the discovery slice (GeoServer CVE-2021-40822)
│   ├── live_solr_discovery.py      # Live TRANSFER validation (Apache Solr RemoteStreaming SSRF)
│   ├── live_flink_discovery.py     # Live SECOND-CLASS validation (Apache Flink path-traversal file read, CVE-2020-17519)
│   ├── live_oob_ssrf_validation.py # Live P6/OOB isolation validation (GeoServer SSRF confirmed out-of-band)
│   ├── live_log4shell_p6_validation.py # Live Log4Shell flagship validation (Solr CVE-2021-44228 confirmed via P6 JNDI/DNS callback)
│   ├── live_capability_learning_validation.py # Live Engagement-A validation of the Layer-2 WRITE side (Solr log4j-core fact 0→1, emission A/B unchanged)
│   ├── live_capability_recall_validation.py # Live two-engagement validation of the Layer-2 READ side (§6 gets-smarter: warm-B recall seeds where cold-control-B finds nothing, live P6 confirms)
│   ├── live_juiceshop_ssrf_discovery.py # Live cross-language generalization (slice A2b): JS-source ingest → EGRESS_FETCH hypothesis → P6-OOB confirm of Juice Shop's own-code SSRF (POST /profile/image/url)
│   ├── live_js_transfer_a2a_validation.py # Deterministic JS→JS transfer (slice A2a): library-borne fact keyed on the package → cold-control(0) vs warm(recall-seeded) + no-over-transfer, raw artifacts to outputs/js-a2a-transfer/
│   ├── live_cross_service_ssrf_validation.py # Live §8 two-service cross-service reachability (slice B1): Juice Shop A→B SSRF, arm b (co-located → finding) vs arm c (generic collaborator → research-lead, the phantom control); raw artifacts to outputs/cross-service-b1/
│   ├── live_cross_service_topology_learning_validation.py # Live §6/§8 two-engagement cross-service topology LEARNING (slice B2): engagement A confirms → abstracted reaches edge written; cold-control B (empty KB, withheld adjacency) 0 xsvc vs warm B (KB from A) 1 catalog-seeded xsvc confirmed by the co-location+P6 gate; zero-FP arm → research-lead; artifacts to outputs/cross-service-b2/
│   ├── test_auth_chain.py
│   └── test_dvwa_exploit_direct.py
├── tests/
│   ├── conftest.py
│   ├── fixtures/                   # Real tool output samples
│   ├── test_tools/                 # Tool wrapper unit tests (incl. resolver chains, binary identity, docker preflight)
│   ├── test_agents/                # Agent logic tests (recon_v2, scan_v2, exploit_v2, research_v2, methodology_xss/sqli, ...)
│   ├── test_comms/                 # Message bus + protocol
│   ├── test_credentials/           # CredentialStore tests
│   ├── test_discovery/             # Discovery-engine unit gate (source ingest, engine chain, carrier + proof seam)
│   ├── test_oob/                   # P6/OOB gate (exfil guardrail, receive-only collaborator, mint/correlate/reap)
│   ├── test_knowledge/             # query.py, persistent_kb.py, payload_loader.py
│   ├── test_llm/                   # Per-provider client tests + resilient fallback
│   ├── test_orchestrator/          # Orchestrator + lifecycle
│   ├── test_integration/           # End-to-end (DVWA pipeline, recon→exploit flow, concurrent agents)
│   ├── test_skills_dvwa/           # Live DVWA skill smoke suite (marker: dvwa_smoke)
│   ├── test_skills_juiceshop/      # Live Juice Shop skill smoke suite (marker: juiceshop_smoke)
│   └── test_state.py
└── docs/
    ├── architecture.md
    ├── adding-tools.md
    ├── playbooks.md
    └── analysis/                   # Threat model, data flow, control flow, semantic review, dependencies
```

## Commands
- `python -m clinkz scan --target <domain> --scope <scope.json>`: Run full pentest
- `python -m clinkz recon --target <domain>`: Run only recon phase
- `pytest tests/`: Run all tests
- `pytest tests/test_tools/test_nmap.py -v`: Run single tool test
- `docker compose -f docker/docker-compose.yml up -d`: Start test targets
- `ruff check src/`: Lint
- `ruff format src/`: Format

## Code Style
- Python 3.12+, use type hints everywhere
- Use Pydantic v2 models for all data structures
- Use async/await for all agent execution, tool calls, and LLM calls
- Use structured logging (Python logging module with JSON formatter)
- Docstrings on all public functions and classes
- Follow Google Python Style Guide

## Key Design Decisions
- **Deterministic agent steps + LLM checkpoints**: Phase agents follow fixed step sequences with the LLM invoked only at named reasoning checkpoints. No free-form ReAct loops in v2.
- **LLM-mediated comms**: Agents NEVER talk directly to each other. All messages go through the Orchestrator.
- **Dynamic lifecycle**: Agents are spun up and shut down by the Orchestrator as needed. An agent can be re-activated if a later phase needs it (capped at `MAX_CROSS_PHASE_RESPINS`).
- **Dynamic tool discovery**: Agents call `ToolResolver.find_tool(capability=...)` — never `resolver.get("nmap")`. Resolver checks MCP servers first, then local CLI tools, then walks declared `TOOL_CHAINS` fallback orders.
- **LLM-agnostic + per-agent providers**: All LLM calls go through `llm/base.py`. Never import openai/anthropic/etc directly in agent code. Each agent has a default provider (`recon/scan/report=gemini`, `exploit/research=anthropic`) overridable via `LLM_PROVIDER_<AGENT>`.
- **Resilient client**: `llm/fallback.py::ResilientLLMClient` rotates providers automatically on rate-limit / timeout, with per-provider retry budgets (`LLM_MAX_RETRIES`).
- **Operation-level timeouts are the safety valve**: every long-running operation is independently bounded so a single hang cannot stall an engagement — per tool subprocess and per HTTP request via `ToolBase._run_subprocess` (`asyncio.wait_for(timeout=self.timeout)`), and per LLM call via a hard `LLM_REQUEST_TIMEOUT` (default 120s; Gemini enforces its own 120s ceiling, Anthropic/OpenAI wrap each call in `asyncio.wait_for`). These matter because the exploit phase has no wall-clock deadline by default.
- **Crawl-safety / session hygiene**: recon/scan must map the target without changing it. `agents/_url_safety.py::is_state_changing_url` gates every crawl visit, endpoint emission, and exploit-plan entry so the shared engagement session is never poisoned by following a WAF/security toggle or logout link.
- **LLM JSON parse resilience**: both the exploit *plan* parse (`_llm_plan_exploits`) and the Step-3 *analysis* parse (`_llm_analyze_results`) tolerate malformed LLM JSON via a shared repair helper (`_repair_and_load` → fence-strip, prose-isolate, trailing-comma/truncation repair, re-balance) plus a single re-prompt for strictly valid JSON; on final failure they fall back deterministically (plan) or to an advisory-empty analysis (analysis) — never crashing the phase.
- **Deterministic skills as contracts**: `_test_*` methods are contracts — if the vulnerability is present, the method MUST find it. Adaptive methodologies (XSS-reflected, SQLi) layer LLM-driven synthesis on top of deterministic phases.
- **Persistent KB feedback loop (Layer-2 capability memory)**: A confirmed discovery-originated finding writes a per-technology **capability fact** (+ observation) to `clinkz_knowledge.db` so future engagements adapt at the capability level where the Δ-model operates. The older technique-success learning loop (`technique_results` + `playbook_entries.success_rate`) is **retired** — superseded, kept read-only for the report's historical view. Confidence is a decayed corroboration PRIOR from confirming observations only; it never gates emission (that stays the P1–P6 proof).
- **Existing parsers preserved**: The ToolBase wrappers and their `parse_output()` implementations remain the local execution backend.
- Tool wrappers return Pydantic models, never raw strings
- Scope enforcement: every tool execution validates targets against scope before running
- **Execution traces**: Each engagement writes `outputs/<engagement_id>/trace.jsonl` capturing tool calls, LLM calls, agent steps, data handoffs, and methodology-phase events. Inspect with `clinkz trace inspect <engagement_id>`.

## Pre-Push Verification
Every change must pass three gates before `git push`. If a gate fails, fix the root cause — never bypass with `--no-verify`, blanket `# noqa`/`# type: ignore`, or skip/xfail added solely to keep CI green.

1. **Lint + Cleanup** — `ruff check src/ tests/` and `ruff format --check src/ tests/`. Use `ruff format src/ tests/` to auto-format. All warnings must be resolved or explicitly justified inline. Beyond ruff, clean up every file the diff touches: remove dead code, fix improper naming, strip stale or unnecessary comments, ensure no hardcoded secrets/credentials, and add `None` guards wherever a `None` can reach a dereference.
2. **Audit (tests)** — the **keyless gate** is `pytest tests/ -q --tb=short --ignore=tests/test_skills_dvwa --ignore=tests/test_skills_juiceshop --ignore=tests/test_pipeline_smoke --ignore=tests/test_integration` for unit/agent/tool/orchestrator tests on every change. It excludes **every** live/container-dependent suite (`test_skills_dvwa`, `test_skills_juiceshop`, `test_pipeline_smoke`, `test_integration`) so it is deterministic and key-free: green means actually green, with or without containers up. A stale exclude list that lets live tests leak in produces flake noise that trains the team to wave off real failures — so the keyless gate must never depend on a container or a provider key. Run the **container gate** separately, against running DVWA :8080 / Juice Shop :3000, when containers are up and the change touches scan/exploit/orchestrator paths (run the live suites serially — they share the target containers): `pytest tests/test_integration/`, `pytest tests/test_skills_dvwa/ -m dvwa_smoke`, `pytest tests/test_skills_juiceshop/ -m juiceshop_smoke`, and `pytest -m pipeline_smoke tests/test_pipeline_smoke/`.
3. **Security review** — Invoke the `security-review` skill (`/security-review`) on the diff whenever the change touches: tool execution (`src/clinkz/tools/`), scope enforcement, credential handling/storage, LLM input/output, HTTP/network/subprocess calls, deserialization, file I/O on user-controlled paths, MCP server interactions, or report rendering. The review must include semantic analysis, control-flow analysis, data-flow analysis, dependency review, and `pip-audit` (add `npm audit` only if JS dependencies exist). Resolve every finding before pushing — never ship command/SQL injection, unsafe deserialization, secret leakage, hardcoded credentials, SSRF, path traversal, or scope-bypass code paths.

Doc/config-only changes (no `.py` modified) may skip gates 1–2, but gate 3 still applies if the change can affect runtime behavior (new permission, new tool entry, new hook, new payload list, etc.).

## Important Rules
- NEVER import a specific LLM SDK outside of the llm/ directory
- NEVER hardcode API keys. Use environment variables via python-dotenv
- NEVER scan targets outside the defined scope
- NEVER have agents communicate directly — all comms go through Orchestrator
- NEVER hardcode tool names in agent code — agents describe capabilities they need, the Tool Resolver finds the right tool
- All tool outputs must be parsed into structured Pydantic models
- Test tool wrappers against real tool output (save sample outputs in tests/fixtures/)
- Keep agent system prompts in separate .md files under prompts/ directories
- Run the **Pre-Push Verification** gates above before every `git push`
- Always push to origin after committing (and after pre-push verification passes)

