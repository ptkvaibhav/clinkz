{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'INPUT=$(cat); CMD=$(echo \"$INPUT\" | jq -r \".tool_input.command // empty\"); if echo \"$CMD\" | grep -q \"git commit\"; then echo \"Running ruff before commit...\" >&2; cd \"$(echo \"$INPUT\" | jq -r \".cwd\")\" && ruff check src/ --fix >&2 && ruff format src/ >&2 || { echo \"{\\\"decision\\\": \\\"block\\\", \\\"reason\\\": \\\"ruff check/format failed. Fix lint errors before committing.\\\"}\"; exit 0; }; fi; echo \"{\\\"decision\\\": \\\"approve\\\"}\"'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'INPUT=$(cat); FILE=$(echo \"$INPUT\" | jq -r \".tool_input.file_path // empty\"); if echo \"$FILE\" | grep -qE \"\\.py$\"; then DIR=$(dirname \"$FILE\"); cd \"$(echo \"$INPUT\" | jq -r \".cwd\")\" && ruff check \"$FILE\" --fix --quiet 2>&1 && ruff format \"$FILE\" --quiet 2>&1; fi; echo \"{\\\"decision\\\": \\\"approve\\\"}\"'"
          }
        ]
      }
    ]
  }
}
